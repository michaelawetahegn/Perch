<?xml version="1.0"?>
<feed xmlns="http://www.w3.org/2005/Atom"
  xml:lang="en-GB">

  <title>Matt Godbolt&#8217;s blog</title>

  <author><name>Matt Godbolt</name></author>

  <id>http://xania.org/feed.atom</id>


  <icon>/favicon.ico</icon>

  <link href="http://xania.org/?utm_source=feed&amp;utm_medium=rss" />
  <link href="http://xania.org/feed.atom" rel="self" />

  <updated>2026-08-05T15:19:00Z</updated>
  <rights>Copyright 2026 Matt Godbolt. Unless otherwise stated, all content is licensed under the Creative Commons Attribution-NonCommercial 2.5 License.</rights>


  <entry>
    <id>tag:xania.org,2007:202608/how-compiler-explorer-runs-on-aws</id>
    <title>How Compiler Explorer Runs on AWS in 2026</title>

    <summary>A tour of the AWS services behind every Compiler Explorer compilation</summary>

    <link href="http://xania.org/202608/how-compiler-explorer-runs-on-aws?utm_source=feed&amp;utm_medium=rss" />

    <updated>2026-08-05T15:19:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written with LLM assistance.<br />Details at end.</p>


<p>I've been meaning to write an update on how <a href="https://godbolt.org">Compiler Explorer</a> actually runs on Amazon's cloud, and it's been sat on my list for a good while now, somewhere behind the other random things that take up what laughably I refer to as my spare time<sup id="fnref:list"><a class="footnote-ref" href="#fn:list">1</a></sup>.</p>

<p>The last time I wrote about this was <a href="/201609/how-compiler-explorer-runs-on-amazon">2016</a>, when the whole site was a load balancer, a couple of instances and some Docker containers I built on my laptop. I wrote a much longer <a href="/202506/how-compiler-explorer-works">how it works</a> last summer, but that one is mostly about Compiler Explorer and only incidentally about the cloud it sits on.</p>

<p>It's no secret that we run on AWS, and none of it is hidden: the <a href="https://github.com/compiler-explorer/infra">infra repository</a> has all the terraform, the install scripts and the <code>ce</code> command line tool we drive the whole thing with, so if you'd rather read the real thing than my description of it, help yourself. So this one goes the other way round, through the Amazon services we lean on, roughly in the order your compile request runs into them.</p>

<h3>Getting your code as far as our servers</h3>

<p>Your browser talks to <a href="https://aws.amazon.com/cloudfront/">CloudFront</a>, Amazon's CDN. We run two separate CloudFront distributions. The one in front of <code>godbolt.org</code> mostly just hands requests on to our load balancer, caching what it sensibly can and compressing things on the way back out<sup id="fnref:brotli"><a class="footnote-ref" href="#fn:brotli">2</a></sup>. The bulky static stuff lives in an S3 bucket behind a second distribution at <code>static.ce-cdn.net</code>: the compiled JavaScript, the images, the web fonts. Much the largest part of that is <a href="https://github.com/microsoft/monaco-editor">Monaco</a>, the editor component out of Visual Studio Code, which is what gives you the syntax highlighting and the squiggly underlines and the rest of it. It's a lot of JavaScript to send someone who only wants to look at some assembly, so it's worth having it cached near them.</p>

<p>Sitting in front of that is <a href="https://aws.amazon.com/waf/">WAF</a>, doing our rate limiting. Our limits are <em>very</em> simple and <em>very</em> high, mostly because we used to be stricter and it kept catching C++ trainers: a whole classroom behind a conference's NAT looks like a single very keen IP address. We could probably do something cleverer with fingerprinting, but raising the limit was easier and it hasn't been a problem since.<sup id="fnref:bans"><a class="footnote-ref" href="#fn:bans">3</a></sup></p>

<h3>Rather a lot of fleets behind one load balancer</h3>

<p>Behind CloudFront is a single <a href="https://aws.amazon.com/elasticloadbalancing/application-load-balancer/" title="Amazon's HTTP-aware load balancer">Application Load Balancer</a>. It works out from the path which cluster a request is for, and then picks a healthy instance in that cluster to send it to. Almost everything has no special prefix at all and goes to the production fleet, which is where the overwhelming bulk of the traffic ends up. <code>/beta*</code> and <code>/staging*</code> go to the beta and staging fleets<sup id="fnref:envs"><a class="footnote-ref" href="#fn:envs">4</a></sup>. <code>/winprod*</code> goes to a fleet of Windows instances running <a href="https://learn.microsoft.com/en-us/cpp/build/reference/compiling-a-c-cpp-program" title="Microsoft's C++ compiler">MSVC</a>, <code>/aarch64prod*</code> to <a href="https://aws.amazon.com/ec2/graviton/" title="Amazon's own ARM64 processors">Graviton</a> machines that run ARM code natively rather than under emulation, and <code>/gpu*</code> to machines with real NVIDIA cards in them<sup id="fnref:gpu"><a class="footnote-ref" href="#fn:gpu">5</a></sup>.</p>

<p>Each of those is an <a href="https://aws.amazon.com/ec2/autoscaling/" title="Amazon's way of keeping N healthy machines alive">Auto Scaling Group</a>, and these days each one is doubled up: a blue and a green. To deploy, we bring up the other colour, wait for it to go healthy, point the load balancer's target group at it and drain the old one. If it's wrong, we point it back. Before that, deploying meant a rolling restart of the fleet with our <code>ce</code> command line tool, one instance at a time. That worked well enough, but backing out a bad release meant rolling the whole fleet forward again onto the previous version, which is a bit slow when you've just broken the site.</p>

<p>The scaling itself is simple: we just try and keep the average CPU load below a threshold. We've kicked around more sophisticated ideas but this one is supported out of the box<sup id="fnref:queuescale"><a class="footnote-ref" href="#fn:queuescale">6</a></sup>.</p>

<h3>Renting the bits nobody else wants</h3>

<p>The production fleet is almost entirely <a href="https://aws.amazon.com/ec2/spot/">spot instances</a> -- unused EC2 capacity, sold off cheap, on the understanding that you can be evicted at two minutes' notice. It's a 60-90% saving over on-demand -- quite a lot of money, for us.</p>

<p>We can get away with that because our instances don't really matter. Everything that needs to survive lives on the shared filesystem, in S3 or in DynamoDB, so an instance that vanishes is just an instance that gets replaced. We ask for sixteen different instance types across the <code>m5</code>, <code>m6</code>, <code>m7</code>, <code>r6</code> and <code>i3</code>/<code>i4i</code> families. The allocation strategy is <code>price-capacity-optimized</code> -- Amazon's way of saying "pick whichever of these is cheapest and least likely to get yanked". In practice we get evicted, the scaling group notices, and a new one turns up.</p>

<h3>Why the compilers are the hard part</h3>

<p>We have around 6,000 compiler entries<sup id="fnref:double"><a class="footnote-ref" href="#fn:double">9</a></sup> across 93 languages, and we never delete any of them<sup id="fnref:never"><a class="footnote-ref" href="#fn:never">7</a></sup>. Once a compiler version goes up it stays up, so that Stack Overflow answer showing a GCC 4.8 codegen quirk still compiles today. It's our bit against link rot, and it does mean we hoard an awful lot of binaries.</p>

<p>All of that lives on <a href="https://aws.amazon.com/efs/">EFS</a>, Amazon's elastic NFS, and NFS has latency. C-like languages pull in an enormous number of very small header files, so the naive version of this is unusably slow. Our fix, which sounds daft, is to build a <a href="https://docs.kernel.org/filesystems/squashfs.html" title="A compressed, read-only Linux filesystem">SquashFS</a> image per compiler, store the image <em>also</em> on EFS, and mount it through a loopback device. The kernel then thinks it's talking to a local block device and caches blocks properly instead of checking with a server in another building every time<sup id="fnref:squash"><a class="footnote-ref" href="#fn:squash">10</a></sup>.</p>

<p>That worked, but it meant mounting a couple of thousand images at every boot, which took the best part of a minute and kept the metadata for thousands of filesystems resident in kernel memory. That cached metadata is a good chunk of why squashfs beats NFS in the first place, so it's not wasted as such; it's just that any one instance is only ever going to touch a handful of those compilers, so we were paying for all of it to use a fraction of it. It took me three years and several abandoned attempts to fix that, and the answer was <a href="/202509/cefs">CEFS</a>: content-addressed images, packed into bundles of around 20GB, mounted on demand by <a href="https://www.kernel.org/doc/html/latest/filesystems/autofs.html" title="The kernel's automounter: mounts a filesystem the first time something touches the path">autofs</a> the first time something touches the path. The migration took us from 2,182 images down to 121, and OS startup from 50 seconds to 20! It's crept back up to around 883 since, because the nightly builds keep making new images and consolidation only packs down whatever is already there. A garbage collection while I was writing this turned up 111 images that nothing references any more, about 63GiB's worth<sup id="fnref:gc"><a class="footnote-ref" href="#fn:gc">8</a></sup>.</p>

<p>Our storage has come down a lot over the year too, though I'm honestly not sure how much of that is CEFS and how much is me finally deleting the old squash images:</p>

<div class="codehilite"><pre><span /><code>Aug/04<span class="w"> </span><span class="m">01</span>:55<span class="w"> </span>admin-node~<span class="w"> </span>$<span class="w"> </span>df<span class="w"> </span>-h
Filesystem<span class="w">                                 </span>Size<span class="w">  </span>Used<span class="w"> </span>Avail<span class="w"> </span>Use%<span class="w"> </span>Mounted<span class="w"> </span>on
/dev/nvme0n1p1<span class="w">                              </span>97G<span class="w">   </span>20G<span class="w">   </span>77G<span class="w">  </span><span class="m">21</span>%<span class="w"> </span>/
fs-db4c8192.efs.us-east-1.amazonaws.com:/<span class="w">  </span><span class="m">8</span>.0E<span class="w">  </span><span class="m">2</span>.2T<span class="w">  </span><span class="m">8</span>.0E<span class="w">   </span><span class="m">1</span>%<span class="w"> </span>/opt
</code></pre></div>


<p>2.2T, where a year ago it was 3.9T. Still nowhere near 8 exabytes, mind.</p>

<h3>Building compilers all night, every night</h3>

<p>We build a pile of compilers from scratch every night: GCC trunk, Clang trunk, and a long tail of experimental branches for reflection, contracts, coroutines and all the other fun stuff. That's 94 nightly build jobs at the moment<sup id="fnref:jobs"><a class="footnote-ref" href="#fn:jobs">11</a></sup>, up from 73 a year ago and 33 in 2022.</p>

<p>Those run on our own <a href="https://github.com/features/actions" title="GitHub's CI system">GitHub Actions</a> runners on <a href="https://aws.amazon.com/ec2/" title="Elastic Compute Cloud, Amazon's virtual machines">EC2</a>, spun up on demand with the excellent <a href="https://github.com/github-aws-runners/terraform-aws-github-runner">terraform-aws-github-runner</a>, with all the compiler orchestration on top being ours. Building trunk LLVM wants a big machine and a decent chunk of time, and GitHub's hosted runners weren't up to it when we set this up.</p>

<p>This is the part of the bill that goes up every time somebody asks us for another compiler and we say yes<sup id="fnref:stale"><a class="footnote-ref" href="#fn:stale">12</a></sup>.</p>

<h3>The bits that are half-finished</h3>

<p>One thing we've built but not finished rolling out is what we call the CE Router: a small fleet whose job is to decide <em>where</em> a compilation should happen, look the answer up in <a href="https://aws.amazon.com/dynamodb/">DynamoDB</a> and drop the request on an <a href="https://aws.amazon.com/sqs/">SQS</a> queue, with the result coming back to your browser over an <a href="https://aws.amazon.com/api-gateway/" title="Amazon's managed API front end">API Gateway</a> WebSocket. The point is to stop the machine that answers the HTTP request having to be the machine that owns the compiler.</p>

<p>It works, but it isn't carrying production traffic yet: the load balancer rules that would send compilations through it are still commented out, and we're migrating gradually. Your compile today still goes straight to a machine in the prod fleet, the way it always has.</p>

<p>Building it did turn up a good AWS gotcha, though. API Gateway WebSocket frames top out at 32KiB, and the assembly for almost any non-trivial program is bigger than that; in the other direction SQS messages cap out at 256KB, which a decent-sized multi-file project will go past. So in both directions, if the thing is too big we shove it in S3 and send a key instead.</p>

<h3>Everything else</h3>

<p>A scattering of other services doing one job each:</p>

<ul>
<li><strong><a href="https://aws.amazon.com/dynamodb/" title="Amazon's key-value store">DynamoDB</a></strong> holds the short links. Every <code>godbolt.org/z/...</code> you've put in a slide deck is a row in a table, and there are a couple of million of them.</li>
<li><strong><a href="https://aws.amazon.com/s3/" title="Simple Storage Service, Amazon's object store">S3</a></strong> holds the compilers we build, the static assets, the logs, and a daily-expiring content-addressable compilation cache, so the second person to compile the same thing gets it for free.</li>
<li><strong><a href="https://aws.amazon.com/lambda/" title="Amazon's run-a-function-without-a-server thing">Lambda</a></strong> does the odd jobs: the <a href="/202505/ai-and-compiler-explorer">Claude Explain</a> backend, the nightly version tracking, and the one that pushes CloudWatch alarms into our Discord so it's not just my phone buzzing at 3am.</li>
<li><strong><a href="https://aws.amazon.com/route53/" title="Amazon's DNS">Route 53</a></strong>, <strong><a href="https://aws.amazon.com/certificate-manager/" title="Certificate Manager, which keeps our TLS certificates current">ACM</a></strong>, <strong><a href="https://aws.amazon.com/cloudtrail/" title="An audit log of everything done to the account">CloudTrail</a></strong>, <strong><a href="https://aws.amazon.com/backup/" title="Scheduled backups of the things we cannot rebuild">Backup</a></strong> and <strong><a href="https://aws.amazon.com/ses/" title="Simple Email Service">SES</a></strong> are the plumbing. I don't think about them much.</li>
<li><strong><a href="https://aws.amazon.com/cloudwatch/" title="Amazon's metrics and alarms">CloudWatch</a></strong> drives the auto-scaling triggers, though for actually looking at things we run <a href="https://grafana.com/" title="Dashboards and graphs">Grafana</a>, <a href="https://prometheus.io/" title="Metrics collection">Prometheus</a> and <a href="https://grafana.com/oss/loki/" title="Log aggregation">Loki</a>. Those <a href="https://stats.compiler-explorer.com/">dashboards are public</a>.</li>
</ul>

<p>Everything is in <code>us-east-1</code>. If you're compiling from Sydney your code goes a long way round, and I'm afraid it's going to keep doing that: keeping a couple of terabytes of compilers in sync across regions would be an absolute nightmare.</p>

<h3>Some numbers</h3>

<p>We log a heavily anonymised JSON record to S3 for every compilation, with a <a href="https://aws.amazon.com/glue/" title="Amazon's data catalogue: lets you run SQL over files in S3">Glue</a> table over the top, so while writing this I went and counted them properly: <strong>5,238,210</strong> compilations in July, and <strong>78.7 million</strong> over the last twelve months. It's been sliding gently all year, down about a third from last autumn's peak.</p>

<p class="picture">
<a href="/202608/compilations-per-month.svg">
<img src="/202608/compilations-per-month.svg" width="800" height="400" alt="Chart of monthly compilations from August 2025 to July 2026, peaking at 7.77 million in October 2025 and falling to 5.24 million in July 2026" />
</a>
<br />Compilations per month over the last year. Generated from <a href="/202608/generate_compilation_chart.py">this script</a>.
</p>


<p>The big drop before that, from 2024's 14 million a month, I can at least half explain: in mid-2024 we doubled the default delay before we auto-recompile as you type, from 750ms to 1500ms, and then to 2 seconds<sup id="fnref:delay"><a class="footnote-ref" href="#fn:delay">13</a></sup>. If you type continuously, doubling that delay roughly halves the number of compilations you generate, without anybody noticing anything much. This latest slide isn't that, though: it started well over a year later.</p>

<p>So I don't really have a good explanation, and I can't easily go and find one, because we deliberately <a href="https://godbolt.org/#privacy">don't track who you are</a>: no cookies, nothing that would let me tell you how many people used the site yesterday. I'd make that trade again every time, but it does leave me squinting at graphs occasionally.</p>

<p>Since I had the query open, here's what folks actually compiled in July:</p>

<div class="codehilite"><pre><span /><code>id                      compiles   compiler
g161                   1,386,532   GCC 16.1 (C++)
cg161                    403,659   GCC 16.1 (C)
gsnapshot                298,256   GCC trunk
clang_trunk              287,885   Clang trunk
g152                     239,331   GCC 15.2
clang2210                177,958   Clang 22.1
vcpp_v19_latest_x64      132,186   MSVC
r1970                     65,380   Rust 1.97
python314                 51,104   Python 3.14
</code></pre></div>


<p>That top entry is <strong>26%</strong> of everything -- roughly what you'd expect when you're the default<sup id="fnref:top10"><a class="footnote-ref" href="#fn:top10">15</a></sup>. The load balancer sees around 14 million requests a month; our busiest day this year was the 21st of April at 1.44 million. The auto-scaling dealt with that one by itself; I only found out about it while writing this.</p>

<p>All of that costs us somewhere around $3,600 a month on AWS at the moment, which works out at about $0.0007 per compilation<sup id="fnref:percomp"><a class="footnote-ref" href="#fn:percomp">14</a></sup>. For the best part of a year we barely paid any of that: AWS's <a href="https://aws.amazon.com/blogs/opensource/aws-promotional-credits-open-source-projects/" title="AWS's promotional credits programme for open source projects">open source credits</a> covered almost all of it. Those ran out in the spring, we paid our own way for a few months, and AWS have just renewed them for another year, which is a huge help. Either way we're fine for money, thanks to our <a href="https://patreon.com/mattgodbolt">Patreon</a> supporters, our <a href="https://github.com/sponsors/mattgodbolt">GitHub sponsors</a> and our <a href="https://godbolt.org/#sponsors">commercial sponsors</a>. I did a <a href="/202506/compiler-explorer-cost-transparency">full breakdown of the costs</a> last year and it's still roughly the right shape.</p>

<p class="picture">
<a href="/202608/compiler-wall-dynamic.svg">
<img src="/202608/compiler-wall-dynamic.svg" width="800" height="600" alt="Visualisation showing 6,157 compilers across 93 languages" />
</a>
<br />The wall of compilers, a year on: 6,157 entries across 93 languages.<br />Regenerated from <a href="/202608/generate_compiler_wall.py">the same script</a> as last year's.
</p>


<h3>Things I'd still like to fix</h3>

<p>Deployment is better than it was but there's still more hand-holding in it than I'd like<sup id="fnref:discovery"><a class="footnote-ref" href="#fn:discovery">16</a></sup>. The CE Router migration has been "nearly done" for a while now... I still wish I'd laid out the NFS directory structure properly at the start instead of growing it one compiler at a time for a decade. And we moved everything to <a href="https://releases.ubuntu.com/noble/" title="Noble Numbat">Ubuntu 24.04</a> this year, which was not the tidy little upgrade it sounds like<sup id="fnref:relr"><a class="footnote-ref" href="#fn:relr">17</a></sup>.</p>

<p>None of this is especially clever: it's a load balancer, some scaling groups full of cheap spot instances, a network filesystem and a decade of accumulated hacks. It does keep working, though, and these days it mostly keeps working without anyone having to go and poke it. I'm pretty happy with that.</p>

<h3>Thanks</h3>

<p>As ever, the people who keep this running are the contributors. <a href="https://bsky.app/profile/partouf.bsky.social">Partouf</a> (Patrick Quist) above all -- I really don't know what CE would do without them -- along with the core team and the many, many people who send PRs and file issues. Thank you all. Thanks too to AWS, whose <a href="https://aws.amazon.com/blogs/opensource/aws-promotional-credits-open-source-projects/">open source credits programme</a> paid for the best part of a year of everything described above, and who have just renewed for another year -- which is not a small thing for a project our size, and it lets us get on with the interesting problems instead of watching the bill. Thank you AWS! And thanks, as always, to our <a href="https://patreon.com/mattgodbolt">Patreon</a> and <a href="https://github.com/sponsors/mattgodbolt">GitHub Sponsors</a> supporters and our <a href="https://godbolt.org/#sponsors">commercial sponsors</a>, who cover the rest.</p>

<p>Questions? Complaints? Compilers we're missing? Drop by our <a href="https://discord.gg/B5WacA7">Discord</a>, or find me on <a href="https://bsky.app/profile/matt.godbolt.org">Bluesky</a> or <a href="https://hachyderm.io/@mattgodbolt">Mastodon</a>.</p>

<hr />

<h3>Disclaimer</h3>

<p>This article was a collaboration between a human and an <a href="https://anthropic.com">LLM</a>. I set it off to dig through our infrastructure repositories and our AWS account for the current numbers, which turned up several things I had wrong, and worked from that. It also ran the Athena queries and regenerated the graphs. The opinions, em- and en-dashes, and mistakes are all mine.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:list">
<p>In my defence, those have included a month back in the UK dealing with some family things, a run of C++ talks (<a href="https://www.youtube.com/watch?v=EU_nQh8wg5A" title="Keynote: Benchmarking -- It's About Time">C++Now</a>, <a href="https://accuonsea.uk/2026/sessions/microarchitecture-what-lies-beneath/" title="Microarchitecture: What Lies Beneath">ACCU on Sea</a>, <a href="https://cppcon.org/" title="The C++ Conference">CppCon</a>, a keynote at <a href="https://cppunderthesea.nl/" title="In Breda, this October">C++ Under the Sea</a>, and <a href="https://meetingcpp.com/meetingcpp/news/items/A-first-release-of-the-program-for-Meeting-Cpp-2026.html" title="My Compiler Explorer talk, in Berlin this November">Meeting C++</a> in Berlin, where I'll be going into a lot of this in rather more detail), some more <a href="https://www.youtube.com/playlist?list=PLzH6n4zXuckpwdGMHgRH5N9xNHzVGCxwf" title="Matt Godbolt on Computerphile">Computerphile</a> videos, and getting completely obsessed with how <a href="https://github.com/mattgodbolt/PALindrome" title="My PAL encoder/decoder">PAL decoding</a> works. <a class="footnote-backref" href="#fnref:list" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:brotli">
<p>We only got round to turning <a href="https://github.com/google/brotli" title="Google's compression algorithm, generally smaller than gzip">brotli</a> compression on at the edge for the main app in June this year. <a class="footnote-backref" href="#fnref:brotli" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:bans">
<p>WAF also gives us a <code>banned-ipv4</code> and a <code>banned-ipv6</code> list that I add to by hand when we spot somebody misbehaving, and take them off again once they've got the hint. It doesn't happen often: last year it was one anomalous IPv6 address, and an analytics outfit that was hammering us, who came off the list again the next day. Every now and again some enterprising soul works out that a site which compiles and runs arbitrary code is, if you squint, a free compute farm, but that's rare and it tends to be pretty obvious. <a class="footnote-backref" href="#fnref:bans" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:envs">
<p>Beta is very rarely used, but when we do have a longer-term experiment on the go that's where it lives. Staging is for short-lived pre-deploy checks. <a class="footnote-backref" href="#fnref:envs" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:gpu">
<p>These cost a small fortune to run, but being able to see the results of running GPU code makes it worthwhile. We worked with our friends at NVIDIA to get the drivers and toolchains working properly, and they were a corporate sponsor for a good while too. Thank you NVIDIA! <a class="footnote-backref" href="#fnref:gpu" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:queuescale">
<p>The obvious better signal is queue depth: how many compilations are actually waiting, rather than how busy the CPUs happen to look. We already have scaling policies written against <code>ApproximateNumberOfMessagesVisible</code> for some of the environments, and once the queue-based compilation path is carrying real traffic everywhere that's what we'd like to scale everything on. <a class="footnote-backref" href="#fnref:queuescale" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
<li id="fn:never">
<p>Nearly never, anyway. Very occasionally an experimental build gets replaced, and the nightly builds replace themselves by definition: today's GCC trunk quietly does away with yesterday's. It's the released versions that stay put forever. <a class="footnote-backref" href="#fnref:never" title="Jump back to footnote 7 in the text">↩</a></p>
</li>
<li id="fn:gc">
<p>Content addressing does at least make that safe to work out: an image that nothing links to is definitely unused, so the collection can be automatic rather than me squinting at filenames and hoping. A good chunk of those 111 will be yesterday's nightly builds, quietly superseded by today's. <a class="footnote-backref" href="#fnref:gc" title="Jump back to footnote 8 in the text">↩</a></p>
</li>
<li id="fn:double">
<p>6,157 if you ask the API, but that's cheating slightly: it counts the same GCC once for C, once for C++, once for Fortran and so on. Unique names, it's 4,291. Last June the equivalent numbers were 4,724 and 81 languages, so we're still growing pretty nicely, and almost none of that is me: folks keep turning up with a language I'd never heard of and a working PR. <a class="footnote-backref" href="#fnref:double" title="Jump back to footnote 9 in the text">↩</a></p>
</li>
<li id="fn:squash">
<p>Normally NFS caches data locally, but it still validates that cached metadata is fresh with the server via attribute caching. Even when the file data is cached you pay the network latency to check it hasn't changed. Going via a loopback-mounted SquashFS image "launders" away the NFS-ness: the squashfs driver sees a local block device, and the kernel is blissfully unaware that behind those cached blocks is a file on NFS that could in theory change. For immutable compiler images this is a huge boon. <a class="footnote-backref" href="#fnref:squash" title="Jump back to footnote 10 in the text">↩</a></p>
</li>
<li id="fn:jobs">
<p>A "job" isn't a compiler: some build one thing, some build a whole family. The builds kick off at midnight UTC and queue on our build infrastructure, and a separate workflow installs at 5:30am, and the two still have no idea about each other, so sometimes we install yesterday's builds. It's on the list. <a class="footnote-backref" href="#fnref:jobs" title="Jump back to footnote 11 in the text">↩</a></p>
</li>
<li id="fn:stale">
<p>We're not completely daft about it: before a nightly build runs it checks whether any of the upstream repositories have had a commit in the last week, and if nothing's moved it skips the build entirely. A quiet week on some experimental branch costs us nothing much. <a class="footnote-backref" href="#fnref:stale" title="Jump back to footnote 12 in the text">↩</a></p>
</li>
<li id="fn:delay">
<p><a href="https://github.com/compiler-explorer/compiler-explorer/issues/6669">#6669</a>, if you want the gory details. It went out at 1500ms, then someone tried 2 seconds, then it got reverted, then it went back to 2 seconds ten days later. The current default is 2000ms. <a class="footnote-backref" href="#fnref:delay" title="Jump back to footnote 13 in the text">↩</a></p>
</li>
<li id="fn:percomp">
<p>Up from $0.00039 a year ago, and not because anything got dearer. The volume went down and the fixed costs -- all that storage, and building all those compilers every night -- stayed exactly where they were. <a class="footnote-backref" href="#fnref:percomp" title="Jump back to footnote 14 in the text">↩</a></p>
</li>
<li id="fn:top10">
<p>I did <a href="/201412/gcc-explorer-top-10">this same exercise back in 2014</a>, out of nearly 2 million compiles <em>in total</em>, to work out whether I could retire GCC 4.4. The default then was clang++ 3.0.6 at 37%, so the shape of the list hasn't changed much even if everything else has. I didn't retire GCC 4.4, and it's still there. <a class="footnote-backref" href="#fnref:top10" title="Jump back to footnote 15 in the text">↩</a></p>
</li>
<li id="fn:discovery">
<p>Mainly it's our "compiler discovery" step: before a version goes out we go and ask every compiler on the machine what it actually is and what it calls itself, and write the answers to a JSON file the site then reads. It's still a manual step and it can occasionally fall over, though I think I've made it rather more stable in a recent commit. I'm also experimenting with using LLM "scripts" to help where I can't automate things directly in code: there are a lot of weird edge cases to handle and encoding all of them properly is tricky. <a class="footnote-backref" href="#fnref:discovery" title="Jump back to footnote 16 in the text">↩</a></p>
</li>
<li id="fn:relr">
<p>Modern glibc uses <code>SHT_RELR</code> relocations, which need <a href="https://www.gnu.org/software/binutils/" title="The GNU assembler, linker and friends">binutils</a> 2.38 or newer to link against, and 29 of our older GCCs ship their own <code>ld</code> that predates that. We tried rebuilding them all from source, which cascaded into a wall of libsanitizer incompatibilities, so we gave up and settled for symlinking those compilers' <code>ld</code> to a newer one at install time instead. Partouf later rebuilt a couple of them properly. <a class="footnote-backref" href="#fnref:relr" title="Jump back to footnote 17 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202605/walking-the-dog</id>
    <title>Walking the dog with Claude</title>

    <summary>An AI interview about explaining hard things simply, conducted on a dog walk</summary>

    <link href="http://xania.org/202605/walking-the-dog?utm_source=feed&amp;utm_medium=rss" />

    <updated>2026-05-24T16:55:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written with LLM assistance.<br />The interview format is genuine; the prose is lightly tidied from voice notes.</p>


<p>I had lunch with a pal yesterday, and we got onto the subject of why
so much technical material is either accurate-but-impenetrable or
polished-but-slightly-wrong. It's a gap I think about a lot, partly
because I make videos that try to land in the middle of it and don't
always succeed. The conversation stuck with me on the drive home.</p>

<p>This morning, whilst walking the dog, I tried something a bit silly: I
asked Claude (dictating to the mobile app<sup id="fnref:expl"><a class="footnote-ref" href="#fn:expl">1</a></sup>) to interview me about it.
Voice-to-text on one end, an LLM
playing journalist on the other, and a black lab setting the pace. What
follows is that conversation, lightly tidied -- paraphrased in places,
verbatim in others. The LLM is the interviewer; I'm the subject.</p>

<p>The words are mine, the questions aren't.</p>

<hr />

<p><strong>Claude:</strong> Let's start wide. Your friend's comment was about the gap
between people who know things and people who can explain them. Where
do you see that gap most clearly?</p>

<p><strong>Matt:</strong> Almost everywhere, honestly. The people with the information
may not be great presenters, and the great presenters don't necessarily
have the information. You end up with two failure modes: dry, accurate
material that nobody wants to watch, or polished material that gets the
details slightly wrong in ways that make experts twitch. Both are
common; the overlap is rarer than I'd like.</p>

<p><strong>Claude:</strong> Who do you think bridges it well?</p>

<p><strong>Matt:</strong> Grady Hillhouse on <a href="https://practical.engineering/">Practical Engineering</a> -- he actually is a
civil engineer, and it shows. Devin Stone, <a href="https://www.youtube.com/@LegalEagle">LegalEagle</a>, is a working
lawyer. And the Brady Haran shows -- <a href="https://www.numberphile.com/">Numberphile</a>, <a href="https://www.youtube.com/user/Computerphile">Computerphile</a> -- solve
it from the other end: Brady and <a href="https://mastodonapp.uk/@seanski44">Sean Riley</a> are well-prepared "straight
man" interviewers, and the experts they bring in are carefully chosen.
The format itself is the trick. I'm biased on Computerphile, mind, since
I've <a href="https://www.youtube.com/playlist?list=PLzH6n4zXuckpwdGMHgRH5N9xNHzVGCxwf">done a few</a>.</p>

<p><strong>Claude:</strong> Let's talk craft. You've mentioned the Usborne robots
before. What are they, and why do they still matter to you?</p>

<p><strong>Matt:</strong> There were these late-70s / early-80s <a href="https://usborne.com/us/books/computer-and-coding-books">Usborne books</a> on how
computers worked, and they used little cartoon robots to show
information moving around inside the machine. As a kid that just
clicked for me. The thing I find quietly remarkable is that I still
use it. When I'm reasoning about out-of-order execution, register
renaming, modern microarchitecture -- somewhere underneath, there are
still little robots shuffling things between pigeon-holes. I'm not
consciously picturing them anymore, but the shape of the model is the
same.</p>

<p><strong>Claude:</strong> That sounds like a beginner's model that you've never quite
outgrown.</p>

<p><strong>Matt:</strong> Maybe you're right? The thing I
keep coming back to is that what's easy for me to reason about as an
expert<sup id="fnref:ugh"><a class="footnote-ref" href="#fn:ugh">2</a></sup> is often <em>exactly</em> what's easy for a beginner to grasp. The
mental model and the explanation aren't two things -- they're the same
object. I think people assume good explanations are simplifications of
the expert's real model. Sometimes they're just the expert's real
model.</p>

<p><strong>Claude:</strong> You've talked about a "tell my mum" test. Where does that
come from?</p>

<p><strong>Matt:</strong> When I was a teenager with a <a href="https://bbc.xania.org/">BBC Micro</a>, if I figured
something out I'd run downstairs and tell my mum. She'd listen and
either follow or politely pretend to. Somewhere along the way that
became my actual yardstick: could I explain this to her? Simple enough
to follow, but <em>not</em> dumbed down. True. And underwritten by the fact
that I actually understand the thing properly, even if I don't expose
all of it. If I can't clear that bar, I'm not ready.</p>

<p><strong>Claude:</strong> And the prep that goes with it?</p>

<p><strong>Matt:</strong> More than people probably realise. For the early
Computerphiles I'd practise on paper, out loud, two or three times,
even though the finished video looks off-the-cuff. The
off-the-cuffness is the prep working.</p>

<p><strong>Claude:</strong> You skipped a Computerphile recording last week. What
happened?</p>

<p><strong>Matt:</strong> Yeah, that's been nagging at me. The topic was how computers
tell the time -- it's fallout from my C++Now keynote on benchmarking<sup id="fnref:kw"><a class="footnote-ref" href="#fn:kw">5</a></sup>.
I wanted to talk about <code>rdtsc</code> and <code>rdtscp</code> on x86, how raw counter
ticks get turned into wall-clock time, and the out-of-order execution
wrinkles that show up when you read the clock. Good material. I just
couldn't find a visual hook. Computerphile's whole stick is hand-drawn,
handwritten stuff on lined paper, and I hadn't worked out how to draw
this one. I'm now mulling writing a tiny x86 assembly snippet
long-hand and using <em>that</em> to show out-of-order behaviour. But the honest
reason I bailed is that I hadn't cleared the mum test yet. I hadn't
reduced it cleanly enough.</p>

<p><strong>Claude:</strong> Why do you do these at all? Be honest.</p>

<p><strong>Matt:</strong> Two layers, I think. The first is that I'm a show-off. My
mum's side of the family are performers -- singers and dancers, mostly --
and I did school musicals, played instruments, all of that. It faded
in my twenties and I've sort of rediscovered it through conference
talks and the videos. I like being on stage. I'm not going to pretend
otherwise.</p>

<p><strong>Claude:</strong> And the second layer?</p>

<p><strong>Matt:</strong> I was giving a guest lecture recently and made the
showing-off joke to a friend afterwards. He said, "I don't think
that's all of it." He's being kind, but he's also right. The bigger
thing is the moment someone <em>gets</em> it. "I know this stuff, and now so
do you -- isn't that cool?" That penny-dropping moment is the actual
fuel. My territory is microarchitecture, compilers, assembly -- how
computers really work, underneath. <a href="/202511/advent-of-compiler-optimisation">Advent of Compiler Optimisations
2025</a> was probably the most concentrated dose of all of this in one go.</p>

<p><strong>Claude:</strong> You've been quieter on the video front lately. Why?</p>

<p><strong>Matt:</strong> Because they cost. And I want to be careful, because the
costs aren't the same shape. Something like Advent of Compiler
Optimisations is genuinely expensive in <em>hours</em> -- between the blog
posts, filming and editing, I <a href="/202512/2025-in-review">reckon</a> about ten hours per 5–10-minute
video, and there were twenty-five of them. Computerphile is much
lighter on time -- an hour or two of prep once the idea's ready. The
harder cost there isn't hours, it's the conceptual distillation. I
can't take it to Sean until it clears the mum bar. Last week's
no-show is the example.</p>

<p><strong>Claude:</strong> Final question, and it's a deliberate provocation.
<a href="https://godbolt.org/">Compiler Explorer</a> takes the most opaque part of writing C++ -- what the
compiler actually does -- and makes it inspectable in real time. Is
that the same impulse as the videos and the talks, just made into a
tool?</p>

<p><strong>Matt:</strong> Do you know what? I'd never actually thought of it that way.
But -- yeah, I suppose there is a bit of that going on. There's the
immediacy I like: it compiles as you type, rather than you having to
kick off some long process. And I try<sup id="fnref:try"><a class="footnote-ref" href="#fn:try">3</a></sup> to keep pushing it to be more
accessible, more instinctive. Put like that, there's clearly overlap.
The tool, the videos and the talks have something of the same flavour I suppose.</p>

<hr />

<p>That last question was quite a surprise; I genuinely hadn't thought of CE that
way before.</p>

<p>I want to do more of these -- more Computerphiles, more talks, more written-up material -- and I
keep not doing them as often as I'd like. The honest reason is the one
above: sometimes the cost is hours, sometimes it's getting the idea
clean enough to be worth saying<sup id="fnref:hrt"><a class="footnote-ref" href="#fn:hrt">4</a></sup>. I'm trying to give myself permission
for the output to be <em>infrequent</em> without it being <em>stopped</em>. Those
are different things, and I keep conflating them.</p>

<p>And -- is this the future? An LLM as an interviewer while you walk the
dog? I'm not sure, but I will say: I came home
with a thought I didn't leave with, which is more than I can say for
most dog walks. Monty remains unconvinced (though he's now snoozing happily on the sofa).</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:expl">
<p>I tried the "conversation mode" but it drives me mad, the thing keeps interrupting me all the time. So I used my phone's own "voice to text" feature and "typed" into the text boxes with it, sending every now and then. Not perfectly seamless, but not bad. <a class="footnote-backref" href="#fnref:expl" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:ugh">
<p>Ugh, I don't really want to accept that mantle but apparently that's what I said... <a class="footnote-backref" href="#fnref:ugh" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:try">
<p>Even though I might not be as good at this as I'd like. <a class="footnote-backref" href="#fnref:try" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:hrt">
<p>And my day job is keeping me busy too! <a class="footnote-backref" href="#fnref:hrt" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:kw">
<p><a href="https://hudson-trading.github.io/its-about-time"><em>Benchmarking: It's About Time -- As-If By Magic</em></a>, if you want to
look it up. The skipped Computerphile was meant to be a domesticated
version of one slice of it. <a class="footnote-backref" href="#fnref:kw" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/2025-in-review</id>
    <title>2025 in Review</title>

    <summary>What has this year all been about, eh?</summary>

    <link href="http://xania.org/202512/2025-in-review?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-31T21:30:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>


<p>2025 has been quite a year for me. The big ticket things for me were having the majority of the year on a non-compete, a new job, and some videos and conference talks.</p>

<p>It was a bumper year for my public talks, which included:</p>

<ul>
<li>ACCU Keynote - <a href="https://www.youtube.com/watch?v=jlt_fScVl50">Teaching an Old Dog New Tricks</a></li>
<li>C++ on Sea - a <a href="https://www.youtube.com/watch?v=gg4pLJNCV9I">reprise</a> of the above talk, slightly shortened</li>
<li>CppCon Keynote - <a href="https://cppcon.org/2025-keynote-matt-godbolt/">C++: Some Assembly Required</a></li>
<li>Jane Street - <a href="https://www.janestreet.com/tech-talks/microarchitecture/">Microarchitecture: What Happens Beneath</a></li>
</ul>

<p>I also appeared in a number of <a href="https://www.youtube.com/playlist?list=PLzH6n4zXuckpwdGMHgRH5N9xNHzVGCxwf">Computerphile videos</a>:</p>

<ul>
<li><a href="https://www.youtube.com/watch?v=PpaQrzoDW2I">Computer Timescales Mapped onto Human Timescales</a></li>
<li><a href="https://www.youtube.com/watch?v=SAk-6gVkio0">How CPU Memory &amp; Caches Work</a></li>
<li><a href="https://www.youtube.com/watch?v=1su3lAh-k4o">Subroutines in Low Level Code</a></li>
<li><a href="https://www.youtube.com/watch?v=tGMSARJk7cA">CPU Interrupts</a></li>
<li><a href="https://www.youtube.com/watch?v=yV4fGeIv7MI">CPU Kernel Mode</a></li>
<li><a href="https://www.youtube.com/watch?v=di5WodGWTiY">Memory Mapping</a></li>
<li><a href="https://www.youtube.com/watch?v=fpkCNu_dzcM">CPU Summary</a></li>
<li><a href="https://www.youtube.com/watch?v=Pu7LvnxV6N0">What is Bootstrapping Anyway?</a></li>
<li><a href="https://www.youtube.com/watch?v=VEyVm6dU55c">How Computers Store Data Serially</a></li>
<li><a href="https://www.youtube.com/watch?v=SJE2lFR5utM">How Ethernet Sends Data</a></li>
</ul>

<p>On the <a href="https://godbolt.org/">Compiler Explorer</a> front, I finally solved a three-year-old problem with <a href="/202509/cefs">CEFS</a> — our new content-addressable filesystem that mounts compiler images on demand instead of all 2,000+ at boot time. I also launched the experimental <a href="https://xania.org/202505/ai-and-compiler-explorer">Claude Explain</a> feature during my CppCon keynote, and wrote about <a href="/202506/how-compiler-explorer-works">how CE works</a> and our <a href="/202506/compiler-explorer-cost-transparency">cost transparency</a>. As always, CE is a community effort and I'm grateful to all the contributors who keep it running.</p>

<p><a href="https://benrady.com/">Ben</a> and I kept up our monthly <a href="https://twoscomplement.org/">Two's Complement</a> podcast, releasing all 12 episodes on schedule. Highlights included pondering <a href="https://twoscomplement.org/#podcast/pair-programming-with-hal">AI pair programming</a>, debating <a href="https://twoscomplement.org/#podcast/c-and-rust-different-tools-for-the-job">C++ and Rust</a>, worrying about <a href="https://twoscomplement.org/#podcast/vibe-coding-and-robot-teammates">vibe coding</a> replacing junior developers, and bonding over <a href="https://twoscomplement.org/#podcast/factorio">Factorio</a>.</p>

<p>I ticked off a bucket list item by appearing in a <a href="https://tom7.org/">Tom7</a> video, alongside such greats as <a href="https://standupmaths.com/">Matt Parker</a> and <a href="https://www.youtube.com/@HBMmaster">jan Misali</a> in a bonkers <a href="https://www.youtube.com/watch?v=4pG8_bWpmaE">Secret Santa video</a>. Such fun, and thank you Santa for my very awesome comic book.</p>

<p>Finally I had my very own series on YouTube - the Advent of Code Optimisation - <a href="https://www.youtube.com/watch?v=eLjZ48gqbyg&amp;list=PL2HVqYf7If8cY4wLk7JUQ2f0JXY_xMQm2">27 videos in all</a>. That was a huge amount of work: with the <a href="/AoCO2025-archive">blog posts</a>, filming and editing I estimate I spent around 10 hours on each 5-10m video!</p>

<p>A very busy year, ending with me starting work at <a href="https://www.hudsonrivertrading.com/">Hudson River Trading</a>. I'm only a month in, but I couldn't be happier with my decision to work here: it's a fantastic place full of impressive technology, interesting problems, and perhaps most importantly, kind and thoughtful human beings.</p>

<p>Here's to whatever excitement 2026 will bring! Happy New Year everyone!</p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by an LLM.</em></p>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/25-thank-you</id>
    <title>Thank you</title>

    <summary>The end of the 2025 Advent of Compiler Optimisation</summary>

    <link href="http://xania.org/202512/25-thank-you?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-25T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>It's the 25th! Whatever you celebrate this time of year, I wish you the very best and hope you are having a lovely day. For me, this is a family time: I'm not at all religious but was brought up to celebrate Christmas. So, today we'll be cooking a massive roast dinner and enjoying family time<sup id="fnref:teenage"><a class="footnote-ref" href="#fn:teenage">1</a></sup>.</p>

<p>This series was an idea I had around this time last year, and it has been a substantial amount of work. I've really enjoyed writing it, and seeing the impact it has had on the compiled language community. I realise now in retrospect I exclusively used C and C++<sup id="fnref:rust"><a class="footnote-ref" href="#fn:rust">2</a></sup>, and concentrated on x86 a bit too much. If I do this again, I'll try and widen my horizons!</p>

<p>Speaking of doing it again, I have a number of half-researched, half-written ideas that didn't make the cut. I may either write a few more non-advent posts exploring them, or keep them for another year. Right now I can't promise anything: most of the work for this series was done while I was between jobs, and now I am <a href="https://www.hudsonrivertrading.com/">working full time</a> I don't know if I'll be able to do this again. Despite the low-budget look of the videos, and the shortness of the posts, it has been a pretty phenomenal amount of work. I don't know how regular content creators do it! I may post a follow-up on "the making of AoCO", and go into some details of how I approached this series.</p>

<p>I'd like to thank a number of people:</p>

<ul>
<li><a href="https://articles.emptycrate.com">Jason Turner</a> for some ideas, feedback and for being such a proponent of Compiler Explorer over the years.</li>
<li><a href="https://www.farside.org.uk/">Malcolm Rowe</a> for his review and encouragement.</li>
<li><a href="https://www.youtube.com/@lauriewired">Laurie Kirk</a> for her help and detailed instructions on getting a decent video setup and editing workflow.</li>
<li><a href="https://bsky.app/profile/seanski44.bsky.social">Sean Riley</a> for invaluable video editing tutoring.</li>
<li><a href="https://www.colinhoad.com/">Colin Hoad</a> whose "Advent of Beeb" videos were the inspiration for this series.</li>
<li>All the folks who work on compilers: GCC, clang, MSVC and the many others. I really only dug into GCC and clang, but without the hard work of compiler maintainers, this series wouldn't exist.</li>
<li>All of Compiler Explorer's Patrons (<a href="https://patreon.com/c/mattgodbolt">join them here</a>), GitHub sponsors (<a href="https://github.com/sponsors/compiler-explorer">join them here</a>), and in particular Greg Barker for his ideas.</li>
<li>The <a href="https://github.com/orgs/compiler-explorer/people">Compiler Explorer Team</a> - a group of lovely people who very kindly give up their time to help administrate the site, triage and fix bugs, and add new features. Without them, Compiler Explorer wouldn't be a tenth of the site it is now.</li>
<li>And of course, my wonderful wife, Ness, for her support over the last few months as I routinely ignored her to write articles and record and edit videos. Thank you - I love you so very much.</li>
</ul>

<p>And of course, thank <strong>you</strong> for joining me on this journey. Compilers are amazing: truly one of the quiet modern miracles that makes the modern world possible!</p>

<p><em>See <a href="https://youtu.be/N1sRfYwzmso">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 25 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/24-cunning-clang">When compilers surprise you</a></em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:teenage">
<p>As much as our teenage children will tolerate, anyway. <a class="footnote-backref" href="#fnref:teenage" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:rust">
<p>In my notes I had at least a couple of Rust ideas, but never got around to them. <a class="footnote-backref" href="#fnref:rust" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/24-cunning-clang</id>
    <title>When compilers surprise you</title>

    <summary>Sometimes compilers can surprise and delight even a jaded old engineer like me</summary>

    <link href="http://xania.org/202512/24-cunning-clang?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-24T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Every now and then a compiler will surprise me with a really smart trick. When I first saw this optimisation I could hardly believe it. I was looking at loop optimisation, and wrote something like this simple function that sums all the numbers up to a given value:</p>

<p>So far so decent: GCC has done some preliminary checks, then fallen into a loop that efficiently sums numbers using <code>lea</code> (we've <a href="/202512/02-adding-integers">seen this before</a>). But taking a closer look at the loop we see something unusual:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L3:</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdx</span><span class="err">+</span><span class="mi">1</span><span class="err">+</span><span class="no">rax</span><span class="p">*</span><span class="mi">2</span><span class="p">]</span><span class="w">        </span><span class="c1">; result = result + 1 + x*2</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="mi">2</span><span class="w">                    </span><span class="c1">; x += 2</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="no">eax</span><span class="w">                  </span><span class="c1">; x != value</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L3</span><span class="w">                       </span><span class="c1">; keep looping</span>
</code></pre></div>


<p>The compiler has cleverly realised it can do two numbers<sup id="fnref:check"><a class="footnote-ref" href="#fn:check">1</a></sup> at a time using the fact it can see we're going to add <code>x</code> <em>and</em> <code>x + 1</code>, which is the same as adding <code>x*2 + 1</code>. Very cunning, I think you'll agree!</p>

<p>If you turn the optimiser up to <code>-O3</code> you'll see the compiler works even harder to vectorise the loop using parallel adds. All very clever.</p>

<p>This is all for GCC. Let's see what clang does with our code:</p>

<p>This is where I nearly fell off my chair: <strong>there is no loop</strong>! Clang checks for positive <code>value</code>, and if so it does:</p>

<div class="codehilite"><pre><span /><code><span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="w"> </span><span class="p">-</span><span class="w"> </span><span class="mi">1</span><span class="p">]</span><span class="w">        </span><span class="c1">; eax = value - 1</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="w"> </span><span class="p">-</span><span class="w"> </span><span class="mi">2</span><span class="p">]</span><span class="w">        </span><span class="c1">; ecx = value - 2</span>
<span class="w">  </span><span class="nf">imul</span><span class="w"> </span><span class="no">rcx</span><span class="p">,</span><span class="w"> </span><span class="no">rax</span><span class="w">             </span><span class="c1">; rcx = (value - 1) * (value - 2)</span>
<span class="w">  </span><span class="nf">shr</span><span class="w"> </span><span class="no">rcx</span><span class="w">                   </span><span class="c1">; rcx &gt;&gt;= 1</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="w"> </span><span class="err">+</span><span class="w"> </span><span class="no">rcx</span><span class="p">]</span><span class="w">      </span><span class="c1">; eax = value + rcx</span>
<span class="w">  </span><span class="nf">dec</span><span class="w"> </span><span class="no">eax</span><span class="w">                   </span><span class="c1">; --eax</span>
<span class="w">  </span><span class="nf">ret</span>
</code></pre></div>


<p>It was not at all obvious to me what on earth was going on here. By backing out the maths a little, this is equivalent to:</p>

<div class="codehilite"><pre><span /><code>v + ((v - 1)(v - 2) / 2) - 1;
</code></pre></div>


<p>Expanding the parentheses:</p>

<div class="codehilite"><pre><span /><code>v + (v² - 2v - v + 2) / 2 - 1
</code></pre></div>


<p>Rearranging a bit:</p>

<div class="codehilite"><pre><span /><code>(v² - 3v + 2) / 2 + (v - 1)
</code></pre></div>


<p>Multiplying the <code>(v - 1)</code> by 2 / 2:</p>

<div class="codehilite"><pre><span /><code>(v² - 3v + 2) / 2 + (2v - 2)/2
</code></pre></div>


<p>Combining those and cancelling:</p>

<div class="codehilite"><pre><span /><code>(v² - v) / 2
</code></pre></div>


<p>Simplifying and factoring gives us <code>v(v - 1) / 2</code> which is the closed-form solution to the "sum of integers"! Truly amazing<sup id="fnref:why"><a class="footnote-ref" href="#fn:why">2</a></sup> - we've gone from an O(n) algorithm as written, to an O(1) one!</p>

<p>I love that despite working with compilers for more than twenty years, they can still surprise and delight me. The years of experience and work that have been poured into making compilers great is truly humbling, and inspiring.</p>

<p>We're nearly at the end of this series - there's so much more to say but that will have to wait for another time. Tomorrow will be a little different: see you then!</p>

<p><em>See <a href="https://youtu.be/V9dy34slaxA">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 24 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/23-switching-it-up">Switching it up a bit</a> | <a href="/202512/25-thank-you">Thank you</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:check">
<p>Some of the initial code checks for odd/even and accounts accordingly. <a class="footnote-backref" href="#fnref:check" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:why">
<p>Why does the compiler emit this exact sequence and not a slightly more straightforward sequence? I think it's partly avoiding overflow in cases where it might otherwise overflow <em>and</em> just a side effect of the way clang tracks and infers <a href="/202512/09-induction-variables">induction variables</a>. I really don't know for sure, though. <a class="footnote-backref" href="#fnref:why" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/23-switching-it-up</id>
    <title>Switching it up a bit</title>

    <summary>Taking a look at the various ways the compiler can optimise switch statements</summary>

    <link href="http://xania.org/202512/23-switching-it-up?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-23T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>The standard wisdom is that switch statements compile to jump tables. And they do - when the compiler can't find something cleverer to do instead.</p>

<p>Let's start with a really simple example:</p>

<p>Here the compiler has spotted the relationship between <code>x</code> and the return value, and rewritten the code as: <code>if (x &lt; 5) return (x+1) * 100; else return 0;</code> - pretty neat. No jump table, just maths!</p>

<p>If we mix up the code a bit so there's no obvious relationship between the input and the return value:</p>

<p>Still no jump table: Now the compiler has built a bespoke lookup table (<code>CSWTCH.1</code>) and then uses <code>x</code> to index into it (after checking it's in bounds).</p>

<p>For "dense" case statements, like the ones above, the compiler can be smart. But even with relatively sparse inputs, the compiler can work its magic. Consider this "is it whitespace?" routine<sup id="fnref:isspc"><a class="footnote-ref" href="#fn:isspc">1</a></sup>:</p>

<p>That <em>still</em> avoids any kind of jump table; and in fact even avoids a branch:</p>

<div class="codehilite"><pre><span /><code><span class="nf">is_whitespace</span><span class="p">(</span><span class="no">char</span><span class="p">):</span>
<span class="w">  </span><span class="nf">sub</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="mi">9</span><span class="w">            </span><span class="c1">; edi = x - 9 (`\t`)</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="mi">8388631</span><span class="w">      </span><span class="c1">; eax = 0b100000000000000000010111</span>
<span class="w">  </span><span class="nf">bt</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rdi</span><span class="w">           </span><span class="c1">; test bit edi in the eax bitmask</span>
<span class="w">  </span><span class="nf">setc</span><span class="w"> </span><span class="no">al</span><span class="w">               </span><span class="c1">; al = (bit was set) ? 1 : 0</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">edx</span><span class="w">          </span><span class="c1">; edx = 0</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">dil</span><span class="p">,</span><span class="w"> </span><span class="mi">24</span><span class="w">           </span><span class="c1">; compare edi with 24</span>
<span class="w">  </span><span class="nf">cmovnb</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">edx</span><span class="w">       </span><span class="c1">; replace al with edx (0) if not below</span>
<span class="w">  </span><span class="nf">ret</span><span class="w">                   </span><span class="c1">; return</span>
</code></pre></div>


<p>The compiler has built a bitmask where each bit says "should we consider this character to be whitespace". To fit the range of bits needed to cover all the whitespace characters, the compiler indexes into the bitmask with <code>(x - 9)</code>. The bit test instruction (<a href="https://www.felixcloutier.com/x86/bt"><code>bt</code></a>) will test any bit position, but our 32-bit bitmask only has meaningful data in positions 0-31. The compiler checks that <code>(x - 9) &lt;= 24</code> to ensure we're within the valid range <sup id="fnref:wrap"><a class="footnote-ref" href="#fn:wrap">2</a></sup> of the bitmask (covering tab at position 0 through space at position 23), and replaces the result with 0 for anything outside this range.</p>

<p>Just to see what else the compiler can generate, let's take a look at both a dense and sparse example that the compiler can't replace with a table (you'll need to scroll around in the Compiler Explorer panes to see more):</p>

<p>For the dense case, the compiler does make a jump table, and indexes by <code>x</code> to jump to the right <code>func</code> routine<sup id="fnref:fall"><a class="footnote-ref" href="#fn:fall">3</a></sup>. For the sparse case, the compiler has to fall back to essentially a set of <code>if()</code> statements, comparing and branching. However, it's clever enough to compare a "mid-range" value first (<code>2511</code>), and if the <code>x</code> value is greater, jumps to code that only looks at the <code>5284</code> and <code>4865</code>. So it's essentially a binary serarch tree of comparisons.</p>

<p>Different compilers employ quite different tricks, so take some time to see what clang does for all the above examples.</p>

<p>Write clear switch statements; let the compiler decide whether that means multiplication, bitmasks, or jump tables. It's pretty darned good at it!</p>

<p><em>See <a href="https://youtu.be/aSljdPafBAw">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 23 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/22-memory-cunningness">Clever memory tricks</a> | <a href="/202512/24-cunning-clang">When compilers surprise you</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:isspc">
<p>Of course you should use <code>isspace()</code>. <a class="footnote-backref" href="#fnref:isspc" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:wrap">
<p>The <code>bt</code> instruction uses the bit position modulo the operand size. In this particular case the compiler emits a <code>bt rax, rdi</code> so values of (x-9) greater than 64 would potentially map onto some of the set bits. <a class="footnote-backref" href="#fnref:wrap" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:fall">
<p>If you comment out the <code>case 4</code>, at least for GCC, you'll see the compiler goes back to compare-and-branch. <a class="footnote-backref" href="#fnref:fall" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/22-memory-cunningness</id>
    <title>Clever memory tricks</title>

    <summary>We learn that compilers have tricks to access memory efficiently</summary>

    <link href="http://xania.org/202512/22-memory-cunningness?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-22T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>After exploring SIMD vectorisation over the last <a href="/202512/20-simd-city">couple</a> of <a href="/202512/21-vectorising-floats">days</a>, let's shift gears to look at another class of compiler cleverness: memory access patterns. String comparisons seem straightforward enough - check the length, compare the bytes, done. But watch what Clang does when comparing against compile-time constants, and you'll see some rather clever tricks involving overlapping memory reads and bitwise operations. What looks like it should be a call to <code>memcmp</code> becomes a handful of inline instructions that exploit the fact that the comparison value is known at compile time<sup id="fnref:gcc"><a class="footnote-ref" href="#fn:gcc">1</a></sup>.</p>

<p>I've set up nine functions that each compare a <code>std::string_view</code> against a constant string of increasing length, from one to nine characters. This gives us a chance to see how the compiler's approach changes based on the length of the comparison.</p>

<p>As we learned when looking at <a href="/202512/16-calling-conventions">calling conventions</a>, a <code>std::string_view</code> is a pointer and a length, passed in two registers on x86 Linux. Each of these functions receives a <code>std::size_t</code> length in <code>rdi</code> and a <code>const char *</code> pointer in <code>rsi</code><sup id="fnref:yes"><a class="footnote-ref" href="#fn:yes">2</a></sup>. One might reasonably expect a call to <code>memcmp</code>, but the compiler has both <a href="/202512/17-inlining-the-ultimate-optimisation">inlined</a> and specialised the comparison for each constant string. Let's take a look at some of these comparison functions, starting with <code>t1</code>:</p>

<div class="codehilite"><pre><span /><code><span class="nl">t1:</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="mi">1</span><span class="w">                </span><span class="c1">; is length 1?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.LBB0_1</span><span class="w">               </span><span class="c1">; if not 1, goto "return false"</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">byte</span><span class="w"> </span><span class="no">ptr</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="p">],</span><span class="w"> </span><span class="mi">65</span><span class="w">    </span><span class="c1">; is the byte 65 ('A')?</span>
<span class="w">  </span><span class="nf">sete</span><span class="w"> </span><span class="no">al</span><span class="w">                   </span><span class="c1">; set result to 0 or 1 accordingly</span>
<span class="w">  </span><span class="nf">ret</span><span class="w">                       </span><span class="c1">; return</span>
<span class="nl">.LBB0_1:</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">eax</span><span class="w">              </span><span class="c1">; set result to false</span>
<span class="w">  </span><span class="nf">ret</span><span class="w">                       </span><span class="c1">; return</span>
</code></pre></div>


<p>We see the length is checked first, and if it's not 1, then we return. Otherwise, we check the one character to see if it's <code>A</code> or not, and then set the return value accordingly. The compiler has used a conditional set <code>sete</code> instruction to avoid a second branch.</p>

<p>The pattern holds for power-of-two sizes: Looking at <code>t2</code>, <code>t4</code> and <code>t8</code> we see that the compiler does the same length check, and then cleverly realises it can compare a 2, 4 or 8-byte value directly with a constant of either <code>AB</code>, <code>ABCD</code> or <code>ABCDEFGH</code> (mouse over the constants in the view to see Compiler Explorer interpret them as ASCII).</p>

<p>Things get more interesting with the 7 character case, <code>t7</code>:</p>

<div class="codehilite"><pre><span /><code><span class="nl">t7:</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="mi">7</span><span class="w">                    </span><span class="c1">; is length 7?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.LBB6_1</span><span class="w">                   </span><span class="c1">; if not, goto "return false"</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="mi">1145258561</span><span class="w">           </span><span class="c1">; set eax to "ABCD"</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">dword</span><span class="w"> </span><span class="no">ptr</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="p">]</span><span class="w">      </span><span class="c1">; eax ^= first four chars of sv</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="mi">1195787588</span><span class="w">           </span><span class="c1">; set ecx to "DEFG"</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="no">dword</span><span class="w"> </span><span class="no">ptr</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="w"> </span><span class="err">+</span><span class="w"> </span><span class="mi">3</span><span class="p">]</span><span class="w">  </span><span class="c1">; ecx ^= chars 3,4,5,6 of sv</span>
<span class="w">  </span><span class="nf">or</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="no">eax</span><span class="w">                   </span><span class="c1">; ecx |= eax</span>
<span class="w">  </span><span class="nf">sete</span><span class="w"> </span><span class="no">al</span><span class="w">                       </span><span class="c1">; result = 1 if "zero flag" else 0</span>
<span class="w">  </span><span class="nf">ret</span><span class="w">                           </span><span class="c1">; return</span>
</code></pre></div>


<p>The check for the length is the same as the other cases, but once we know we're going to be comparing 7 bytes, some cunning tricks come into play. First, the compiler isn't directly comparing, as you might expect: It uses the fact that XORing identical values will result in a zero. Secondly, it has used two <em>overlapping</em> reads - reading bytes 0,1,2,3 and then 3,4,5,6. The redundant read of byte 3 doesn't matter, but doing two 32-bit reads is cheaper than having to read individual bytes.</p>

<p>Once the two XORs have happened, we have "zero only if first four bytes match ABCD" in <code>eax</code> and "zero only if bytes 3,4,5,6 match DEFG" in <code>ecx</code>. Simply logical-ORing the two together gives us zero if and only if both were zero - only if all bytes matched. Then a simple <code>sete</code> turns the "zero flag" into either 0 or 1 for the <code>true</code>/<code>false</code> return value needed. Cute!</p>

<p>This optimisation works well on x86 as reading unaligned 32-bit values is free. You can play around with the compiler choice and see what neat tricks are conjured up by different compilers and architecture choices.</p>

<p>And that's what makes modern compilers remarkable - all this cleverness is conjured up from a simple <code>sv == "ABCDEFG"sv</code>. The overlapping reads, the XOR operations, the branchless conditionals - they're all applied automatically. Your job is to write clear code; the compiler's job is to make it fast. Leave it to do its thing, and try not to get in its way!</p>

<p><em>See <a href="https://youtu.be/kXmqwJoaapg">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 22 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/21-vectorising-floats">When SIMD Fails: Floating Point Associativity</a> | <a href="/202512/23-switching-it-up">Switching it up a bit</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:gcc">
<p>GCC generates more obvious, but slightly worse code, with some unnecessary logic operations. I <a href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=122315">filed a bug</a> to investigate. <a class="footnote-backref" href="#fnref:gcc" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:yes">
<p>libstdc++'s <code>std::string_view</code> is defined as <a href="https://gcc.gnu.org/onlinedocs/gcc-15.2.0/libstdc++/api/a00227_source.html#l00102">length then pointer</a>, which is why we see length in <code>rdi</code> before pointer in <code>rsi</code>. <a class="footnote-backref" href="#fnref:yes" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/21-vectorising-floats</id>
    <title>When SIMD Fails: Floating Point Associativity</title>

    <summary>Why floating point maths doesn't vectorise like integers, and what to do about it</summary>

    <link href="http://xania.org/202512/21-vectorising-floats?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-21T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p><a href="/202512/20-simd-city">Yesterday</a> we saw SIMD work beautifully with integers. But floating point has a surprise in store. Let's try summing an array<sup id="fnref:sum"><a class="footnote-ref" href="#fn:sum">1</a></sup>:</p>

<p>Looking at the core loop, the compiler has pulled off a clever trick:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L2:</span>
<span class="w">  </span><span class="c1">; pick up 8 integers and add them element-wise to ymm0</span>
<span class="w">  </span><span class="nf">vpaddd</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="p">]</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="mi">32</span><span class="w">       </span><span class="c1">; move to the next batch of integers</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rdi</span><span class="w">      </span><span class="c1">; at the end?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L2</span><span class="w">           </span><span class="c1">; loop if not</span>
</code></pre></div>


<p>The compiler is using a vectorised add instruction which treats the <code>ymm0</code> as 8 separate integers, adding them up individually to the corresponding elements read from <code>[rdi]</code>. That's incredibly efficient, processing 8 integers per loop iteration, but at the end of that loop we'll have 8 separate subtotals. The compiler adds a bit of "fix up" code after the loop to sum all these 8 subtotals:</p>

<div class="codehilite"><pre><span /><code><span class="w">  </span><span class="nf">vextracti128</span><span class="w"> </span><span class="no">xmm1</span><span class="p">,</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="mi">0x1</span><span class="w">  </span><span class="c1">; xmm1 = ymm0 &gt;&gt; 128</span>
<span class="w">  </span><span class="nf">vpaddd</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm1</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="w">       </span><span class="c1">; xmm0 += xmm1</span>
<span class="w">  </span><span class="nf">vpsrldq</span><span class="w"> </span><span class="no">xmm1</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="mi">8</span><span class="w">         </span><span class="c1">; xmm1 = xmm0 &gt;&gt; 64</span>
<span class="w">  </span><span class="nf">vpaddd</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm1</span><span class="w">       </span><span class="c1">; xmm0 += xmm1</span>
<span class="w">  </span><span class="nf">vpsrldq</span><span class="w"> </span><span class="no">xmm1</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="mi">4</span><span class="w">         </span><span class="c1">; xmm1 = xmm0 &gt;&gt; 32</span>
<span class="w">  </span><span class="nf">vpaddd</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm1</span><span class="w">       </span><span class="c1">; xmm0 += xmm1</span>
<span class="w">  </span><span class="nf">vmovd</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="w">               </span><span class="c1">; return xmm0</span>
</code></pre></div>


<p>This sequence repeatedly adds the "top half" of the result to the bottom half until there's only one left. This fix up code is a little bit extra work, but the efficiency of the loop makes up for it<sup id="fnref:esp"><a class="footnote-ref" href="#fn:esp">2</a></sup>.</p>

<p>Let's switch to <code>float</code>s<sup id="fnref:hmm"><a class="footnote-ref" href="#fn:hmm">3</a></sup> and see what happens:</p>

<p>We're still processing 32 bytes' worth of <code>float</code>s per loop iteration<sup id="fnref:cheat"><a class="footnote-ref" href="#fn:cheat">4</a></sup>, but something unfortunate is going on:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L2:</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="p">]</span><span class="w">    </span><span class="c1">; xmm0 += first elem</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="mi">32</span><span class="w">                           </span><span class="c1">; move to next 8 floats</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-28</span><span class="p">]</span><span class="w"> </span><span class="c1">; xmm0 += second</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-24</span><span class="p">]</span><span class="w"> </span><span class="c1">; etc</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-20</span><span class="p">]</span><span class="w"> </span><span class="c1">; ...</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-16</span><span class="p">]</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-12</span><span class="p">]</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-8</span><span class="p">]</span>
<span class="w">  </span><span class="nf">vaddss</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">xmm0</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-4</span><span class="p">]</span><span class="w">  </span><span class="c1">; xmm0 += eighth</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rdi</span><span class="w">                          </span><span class="c1">; at the end?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L2</span><span class="w">                               </span><span class="c1">; loop if not</span>
</code></pre></div>


<p>The compiler has chosen to do 8 floats per loop iteration...but then proceeded to do each add individually. What has happened here?</p>

<p>To understand we need to recall that maths on <code>float</code>s is special: Operations are not associative. When adding integers, you can regroup the operations however you like—<code>(x + y) + z</code> gives the same result as <code>x + (y + z)</code>. Not so with <code>float</code>s: after each operation, rounding occurs. Depending on the relative magnitudes of <code>x</code>, <code>y</code> and <code>z</code>, regrouping the additions will change the result.</p>

<p>When our compiler rewrote our integer sum loop into eight subtotals to take advantage of SIMD, and then summed the subtotals, it was regrouping the additions. We wrote a loop that summed from the first element to the last, and the compiler changed it to "accumulate every 8th element in 8 separate groups, then add those together". That works for integers but not floating point numbers.</p>

<p>So - are we stuck? Perhaps we know we don't care about associativity? Some of you may be itching to use the <code>-Ofast</code> or <code>-funsafe-math-optimizations</code><sup id="fnref:fun"><a class="footnote-ref" href="#fn:fun">5</a></sup> flag here.  Both flags give the compiler leeway to globally ignore the rules about floating-point maths and both will work<sup id="fnref:global"><a class="footnote-ref" href="#fn:global">6</a></sup>. However, there's a better, more targeted way:</p>

<p>In this example, we tell GCC to "assume maths is associative" for just the function <code>sum</code> by using an annotation. That means the effects are limited to just that one function, and will be applied consistently regardless of how this function is compiled or used. Yes; it does rely on a GCC extension, which is unfortunate. In theory we can use <a href="https://en.cppreference.com/w/cpp/algorithm/reduce.html"><code>std::reduce</code></a> with an unsequenced execution policy, but in my testing that didn't work here.</p>

<p>Floating point maths has some unusual and perhaps surprising gotchas. Knowing about them can help you work with the compiler to get super fast vectorised code.</p>

<p><em>See <a href="https://youtu.be/lUTvi_96-D8">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 21 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/20-simd-city">SIMD City: Auto-vectorisation</a> | <a href="/202512/22-memory-cunningness">Clever memory tricks</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:sum">
<p>Yes, I know, this is what <code>std::accumulate</code> and similar are for, and you'll see similar code generation if you use them. <a class="footnote-backref" href="#fnref:sum" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:esp">
<p>Especially in this situation where the compiler can see we have 65536 elements to process. If we had a variable number of elements, the compiler might add code to conditionally vectorise based on the number - which it has to do anyway to check if there are fewer than 8 elements, anyway. <a class="footnote-backref" href="#fnref:esp" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:hmm">
<p>If you go back to yesterday's post and switch to <code>float</code>s, you'll see the compiler is able to <code>max</code> with vectors just as well as integers. <a class="footnote-backref" href="#fnref:hmm" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:cheat">
<p>My heuristic to see if the compiler has vectorised is to find the loop and then look at how much the compiler adds to the loop counter: it's a good starting point for telling if we're doing more than one element at a time. In this case though, it's slightly misleading. <a class="footnote-backref" href="#fnref:cheat" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:fun">
<p>My favourite flag to the compiler, though it is neither fun nor safe. <a class="footnote-backref" href="#fnref:fun" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:global">
<p>You can edit the flags in the example and see it work. However, both affect the program/compiled translation unit globally. That can lead to some unfortunate side effects outside of the one or two functions you <em>actually</em> want this applied to. Chandler Carruth refers to this as "unbounded precision loss" and he's not wrong. <a class="footnote-backref" href="#fnref:global" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/20-simd-city</id>
    <title>SIMD City: Auto-vectorisation</title>

    <summary>Doing more with less: vectorising can speed your code up 8x or more!</summary>

    <link href="http://xania.org/202512/20-simd-city?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-20T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>It's time to look at one of the most sophisticated optimisations compilers can do: autovectorisation. Most "big data" style problems boil down to "do this maths to huge arrays", and the limiting factor isn't the maths itself, but the feeding of instructions to the CPU, along with the data it needs.</p>

<p>To help with this problem, CPU designers came up with SIMD: "Single Instruction, Multiple Data". One instruction tells the CPU what to do with a whole chunk of data. These chunks could be 2, 4, 8, 16 or similar units of integers or floating point values, all treated individually. Initially<sup id="fnref:games"><a class="footnote-ref" href="#fn:games">1</a></sup> the only way to use this capability was to write assembly language directly, but luckily for us, compilers are now able to help.</p>

<p>To take advantage of SIMD we need to ensure our data is laid out in a nice long line, like an array, or vector<sup id="fnref:vectorisation"><a class="footnote-ref" href="#fn:vectorisation">3</a></sup>. It's also much better to store different "fields" of the data in separate arrays<sup id="fnref:acton"><a class="footnote-ref" href="#fn:acton">2</a></sup>. We're going to start with some integer maths - let's update an array <code>x</code> to be element-wise the max of <code>x</code> and <code>y</code>:</p>

<p>In this <code>-O2</code>-compiled code<sup id="fnref:temp"><a class="footnote-ref" href="#fn:temp">4</a></sup>, we don't see any vectorisation, just the nice tight loop code we've come to expect:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L3:</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="err">+</span><span class="no">rax</span><span class="p">*</span><span class="mi">4</span><span class="p">]</span><span class="w">    </span><span class="c1">; read y[i]</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rax</span><span class="p">*</span><span class="mi">4</span><span class="p">]</span><span class="w">    </span><span class="c1">; compare with x[i]</span>
<span class="w">  </span><span class="nf">jle</span><span class="w"> </span><span class="no">.L2</span><span class="w">                           </span><span class="c1">; if less, skip next instr</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rax</span><span class="p">*</span><span class="mi">4</span><span class="p">],</span><span class="w"> </span><span class="no">edx</span><span class="w">    </span><span class="c1">; x[i] = y[i]</span>
<span class="nl">.L2:</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">1</span><span class="w">                        </span><span class="c1">; ++i</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">65536</span><span class="w">                    </span><span class="c1">; are we at 65536?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L3</span><span class="w">                           </span><span class="c1">; keep looping if not</span>
</code></pre></div>


<p>To ramp up to the next level, we'll need to add two flags: first we need to turn up the optimiser to <code>-O3</code><sup id="fnref:or"><a class="footnote-ref" href="#fn:or">5</a></sup>, and then we need to tell the compiler to target a CPU that has the appropriate SIMD instruction<sup id="fnref:most"><a class="footnote-ref" href="#fn:most">6</a></sup> with something like <code>-march=skylake</code>.</p>

<p>Our inner loop has gotten a little more complex, but spot the cool part:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L4:</span>
<span class="w">  </span><span class="c1">; Reads 8 integers into ymm1, y[i..i+7]</span>
<span class="w">  </span><span class="nf">vmovdqu</span><span class="w"> </span><span class="no">ymm1</span><span class="p">,</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="err">+</span><span class="no">rax</span><span class="p">]</span>
<span class="w">  </span><span class="c1">; Compares that with 8 integers x[i..i+7]</span>
<span class="w">  </span><span class="nf">vpcmpgtd</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">ymm1</span><span class="p">,</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rax</span><span class="p">]</span>
<span class="w">  </span><span class="c1">; Were all 8 values less or equal?</span>
<span class="w">  </span><span class="nf">vptest</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">ymm0</span>
<span class="w">  </span><span class="c1">; if so, skip the write</span>
<span class="w">  </span><span class="nf">je</span><span class="w"> </span><span class="no">.L3</span>
<span class="w">  </span><span class="c1">; mask move x[i..i+7] with the y values that were greater</span>
<span class="w">  </span><span class="nf">vpmaskmovd</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rax</span><span class="p">],</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">ymm1</span>
<span class="nl">.L3:</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">32</span><span class="w">           </span><span class="c1">; move forward 32 bytes</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">262144</span><span class="w">       </span><span class="c1">; are we at the end? (4*65536)</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L4</span><span class="w">               </span><span class="c1">; keep looping if not</span>
</code></pre></div>


<p>With a very similar number of instructions we're now processing 8 integers at a time! This is the power of SIMD - we'd likely be limited by the bandwidth to memory rather than the housekeeping of working out what to do with them.</p>

<p>There are a couple of things to talk about though. Firstly, what's all this "mask move" stuff? Well, with SIMD we can act on "multiple data" but only with a "single instruction". Our original code has a conditional update: not every array element is necessarily processed in the same way. The compiler has cleverly used a "mask move" to let it <em>conditionally</em> write back only the elements that are "greater than", which is pretty clever of it. We can help it if we don't mind unconditionally writing to memory:</p>

<p>By unconditionally writing to <code>x[i]</code><sup id="fnref:ok"><a class="footnote-ref" href="#fn:ok">7</a></sup> we can reduce the loop to:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L3:</span>
<span class="w">  </span><span class="c1">; Read 8 integers from x[i..i+7]</span>
<span class="w">  </span><span class="nf">vmovdqu</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rax</span><span class="p">]</span>
<span class="w">  </span><span class="c1">; "max" the integers with y[i..i+7]</span>
<span class="w">  </span><span class="nf">vpmaxsd</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">ymm0</span><span class="p">,</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="err">+</span><span class="no">rax</span><span class="p">]</span>
<span class="w">  </span><span class="c1">; Store them back</span>
<span class="w">  </span><span class="nf">vmovdqu</span><span class="w"> </span><span class="no">YMMWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rax</span><span class="p">],</span><span class="w"> </span><span class="no">ymm0</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">32</span><span class="w">       </span><span class="c1">; move forward 32 bytes</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">262144</span><span class="w">   </span><span class="c1">; are we at the end?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L3</span><span class="w">           </span><span class="c1">; keep looping if not</span>
</code></pre></div>


<p>The compiler has spotted our ternary operator is actually exactly the same as the built-in vector "max" instruction! No explicit comparisons or branches now - fantastic.</p>

<p>There's one last thing to cover - the eagle-eyed amongst you might have spotted the fact there are actually <em>two</em> loops, one using vector instructions, and one doing regular one-at-a-time operations. The regular loop is jumped to conditionally after this check:</p>

<div class="codehilite"><pre><span /><code><span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-4</span><span class="p">]</span><span class="w">      </span><span class="c1">; rax = x - 4</span>
<span class="w">  </span><span class="nf">sub</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rsi</span><span class="w">          </span><span class="c1">; rax = x - 4 - y</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">24</span><span class="w">           </span><span class="c1">; compare rax with 24</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="mi">0</span><span class="w">            </span><span class="c1">; (set up loop counter for below)</span>
<span class="w">  </span><span class="nf">jbe</span><span class="w"> </span><span class="no">.L2</span><span class="w">               </span><span class="c1">; if rax &lt;= 24; jump to the slow case</span>
<span class="c1">; falls through to the vectorised loop here</span>
</code></pre></div>


<p>So what's going on here? We're checking <code>x</code> and <code>y</code> - not the <em>values</em> of the two arrays, but the addresses. That somewhat awkward sequence is essentially saying "do the two arrays overlap with each other in a way that would prevent us being able to vectorise". If they overlap by 28 or more bytes, then we can't read and write in 8 <code>int</code> (32-byte) chunks: working in batches like this would give different results as the overlap means writing to <code>x[i]</code> might affect <code>y[i+2]</code> (or similar). So, the compiler has to add this check <em>and</em> generate a fall-back case that does one at a time.</p>

<p>With some non-standard trickery we tell the compiler to ignore vector dependencies for the loop<sup id="fnref:restrict"><a class="footnote-ref" href="#fn:restrict">8</a></sup>:</p>

<p>That's it for today: with the right flags and a little care about data layout, the compiler can turn your one-at-a-time loops into batch-processing powerhouses! Tomorrow we'll look at floating point vectorisation and its own peculiar quirks.</p>

<p><em>See <a href="https://youtu.be/d68x8TF7XJs">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 20 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/19-tail-call-optimisation">Chasing your tail</a> | <a href="/202512/21-vectorising-floats">When SIMD Fails: Floating Point Associativity</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:games">
<p>I spent some of the late 90s and early 2000s writing video games, whose main rendering loops need to do a lot of matrix maths on a lot of 3d points. Writing big chunks of SIMD-like assembly code was a big part of that. You can see some of my code from that era in the <a href="https://github.com/mattgodbolt/reddog">Red Dog source</a>. <a class="footnote-backref" href="#fnref:games" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:acton">
<p>See Mike Acton's seminal video on <a href="https://www.youtube.com/watch?v=rX0ItVEVjHc">Data Oriented Design</a> <a class="footnote-backref" href="#fnref:acton" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:vectorisation">
<p>I mean, it's also called "vectorisation" for a reason, eh? This has great benefits for caching too: a long sequence is best-case for the automatic prefetchers in modern CPUs. <a class="footnote-backref" href="#fnref:vectorisation" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:temp">
<p>In "real" code we'd make <code>Size</code> a template parameter, but without an instantiation we'd see no code. To keep things brief, I'm hardcoding it to a constant here. <a class="footnote-backref" href="#fnref:temp" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:or">
<p>Or, more targetedly, use <code>-ftree-loop-vectorize</code> <a class="footnote-backref" href="#fnref:or" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:most">
<p>Almost every CPU made in the last decade supports it, but the default target of GCC doesn't have the masked instructions the compiler wants to use. <a class="footnote-backref" href="#fnref:most" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
<li id="fn:ok">
<p>This <em>might</em> be OK if we think we'll need to update many of the x array elements anyway. If we expect not to update much of <code>x</code> then this could be a pessimisation as we unconditionally write to the whole array now. <a class="footnote-backref" href="#fnref:ok" title="Jump back to footnote 7 in the text">↩</a></p>
</li>
<li id="fn:restrict">
<p>Ideally we'd have a way in standard to say "these don't overlap". Try as I might, without making things a lot more complex, neither <code>__restrict</code> on pointers derived from the span nor <code>[[assume(...)]]</code> could convince the compiler that it didn't need to be conservative. Splitting the function into two, one taking <code>__restrict__</code> pointers and the other passing <code>x.data()</code> did work but was horrible. <a class="footnote-backref" href="#fnref:restrict" title="Jump back to footnote 8 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/19-tail-call-optimisation</id>
    <title>Chasing your tail</title>

    <summary>The art of not (directly) coming back: tail call optimisation</summary>

    <link href="http://xania.org/202512/19-tail-call-optimisation?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-19T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Inlining is fantastic, as we've <a href="/202512/17-inlining-the-ultimate-optimisation">seen</a> <a href="/202512/18-partial-inlining">recently</a>. There's a place it surely can't help though: recursion! If we call our own function, then surely we can't inline...</p>

<p>Let's see what the compiler does with the classic recursive "greatest common divisor" routine - surely it can't avoid calling itself?</p>

<p>And yet: The compiler is able to avoid the recursion! When a function ends by calling another function (including itself), the compiler is often able to replace the call with a jump to that function. This is called "tail call optimisation". If that function is itself, the "jump to the start" is effectively the same as a loop. Our compiler turns our recursive function into a loop:</p>

<div class="codehilite"><pre><span /><code><span class="nf">gcd</span><span class="p">(</span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">,</span><span class="w"> </span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">):</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">edi</span><span class="w">      </span><span class="c1">; eax (return val) = a</span>
<span class="w">  </span><span class="nf">test</span><span class="w"> </span><span class="no">esi</span><span class="p">,</span><span class="w"> </span><span class="no">esi</span><span class="w">     </span><span class="c1">; is b == 0?</span>
<span class="w">  </span><span class="nf">je</span><span class="w"> </span><span class="no">.LBB0_4</span><span class="w">        </span><span class="c1">; if b is zero, jump to the end</span>

<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">esi</span><span class="w">      </span><span class="c1">; edx = b</span>
<span class="nl">.LBB0_2:</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="no">edx</span><span class="w">      </span><span class="c1">; ecx = b</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">edx</span><span class="w">      </span><span class="c1">; edx = 0 (set up for divide)</span>
<span class="w">  </span><span class="nf">div</span><span class="w"> </span><span class="no">ecx</span><span class="w">           </span><span class="c1">; eax = edx:eax (0:a) / ecx (b)</span>
<span class="w">                    </span><span class="c1">; edx = edx:eax (0:a) % ecx (b)</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">ecx</span><span class="w">      </span><span class="c1">; eax = b</span>
<span class="w">  </span><span class="nf">test</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">edx</span><span class="w">     </span><span class="c1">; was a % b == 0?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.LBB0_2</span><span class="w">       </span><span class="c1">; if not, keep looping</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">ecx</span><span class="w">      </span><span class="c1">; eax (return val) = b</span>
<span class="nl">.LBB0_4:</span>
<span class="w">  </span><span class="nf">ret</span>
</code></pre></div>


<p>Pretty neat, eh? In this particular case the fact that we are recursing also unlocked further optimisations<sup id="fnref:fact"><a class="footnote-ref" href="#fn:fact">2</a></sup>, but in general, tail recursion is useful to save a few instructions and some stack space<sup id="fnref:gcc"><a class="footnote-ref" href="#fn:gcc">1</a></sup>.</p>

<p>Sometimes we need to guarantee that tail calls happen<sup id="fnref:args"><a class="footnote-ref" href="#fn:args">3</a></sup>. Clang has a way to force tail call optimisation or error if it's not possible: the <code>[[clang::musttail]]</code> annotation. In our gcd example, without tail-call optimisations we need a lot more stack space. To guarantee that tail calls happen (even in debug mode) we can specify this annotation.</p>

<p>Tail call optimisation saves stack space, enables loop optimisations, and can even make your branch predictor happier. All this from a compiler noticing your function ends with a call and thinking "why bother coming back?" Sometimes the best optimisation is to just keep forging ahead!</p>

<p>One last point worth mentioning, tying back to <a href="/202512/16-calling-conventions">calling conventions</a>: if you arrange for your tail-called function's arguments to be in the same order (or partial order) as the calling function's parameters, the compiler can be super efficient. Compare these two functions:</p>

<p>Our same order function simply jumps on to <code>wrapped_func</code> even though we take <code>z</code> too - the compiler can ignore it without issue. For the switched args version, the compiler has to emit instructions to switch parameters around, which obviously is more work. Sometimes that's unavoidable, of course, but it's worth keeping in mind when designing your function interfaces.</p>

<hr />

<h3>Bonus: TCOs in emulators</h3>

<p><strong>Note</strong>: This section explores a more advanced application of TCO. If you're feeling overwhelmed, feel free to skip it - the core TCO concepts are all covered above.</p>

<p>One surprising application of tail call optimisation is in CPU emulators<sup id="fnref:emu"><a class="footnote-ref" href="#fn:emu">4</a></sup>. Most emulators use a <code>switch</code> statement inside a loop - fetch an opcode byte, switch on it to handle the instruction, repeat. That's simple and works well.</p>

<p>But there's an alternative approach using tail calls<sup id="fnref:trick"><a class="footnote-ref" href="#fn:trick">5</a></sup>:</p>

<p>Each instruction handler calls <code>next_opcode()</code>, which looks up and calls the next handler. This looks like unbounded recursion! But the compiler inlines <code>next_opcode()</code> and uses TCO, turning it into a jump. Each handler ends with an indirect <code>jmp</code> through the opcode table - no stack growth at all.</p>

<p>Why bother with this complexity? Branch prediction! Modern CPUs predict branches based on history and branch path, indexed by each branch location. With a <code>switch</code> statement, there's <em>one</em> branch trying to predict which of 256 instructions comes next - an impossible task. In my emulators, that single branch accounts for a double-digit percentage of <em>all</em> mispredictions.</p>

<p>With the TCO approach, each of the 256 instruction handlers has its own branch, each with its own predictor state. The branch at the end of the 6502 <code>CMP</code> can learn "often followed by <code>BNE</code>", while the branch at the end of <code>LDA</code> learns different patterns. This gives the CPU's branch predictor much better information to work with.</p>

<p>It's a niche optimisation, but a neat example of how TCO enables techniques that would otherwise be impractical!</p>

<p><em>See <a href="https://youtu.be/J1vtP0QDLLU">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 19 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/18-partial-inlining">Partial inlining</a> | <a href="/202512/20-simd-city">SIMD City: Auto-vectorisation</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:gcc">
<p>Interestingly, gcc generated a number of redundant extra instructions, so I ended up filing a <a href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=122272">bug</a>! <a class="footnote-backref" href="#fnref:gcc" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:fact">
<p>Similar optimisations can take a recursive factorial (<code>return x * fact(x-1);</code>) into iteration too. Compilers are fab. <a class="footnote-backref" href="#fnref:fact" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:args">
<p>TCO is most efficient when the tail-called function's parameters happen to already be in the right registers. As we saw when looking at <a href="/202512/16-calling-conventions">calling conventions</a>, which parameters end up in registers (and which registers) is determined by the ABI. <a class="footnote-backref" href="#fnref:args" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:emu">
<p>This trick applies to bytecode interpreters too: LUA, DWARF debug info, stack unwinding, etc. My emulators: <a href="https://bbc.xania.org">jsbeeb</a> (BBC Micro), <a href="https://miracle.xania.org">Miracle</a> (Sega Master System), <a href="https://spectrum.xania.org">specbolt</a> (ZX Spectrum in C++/WASM). <a class="footnote-backref" href="#fnref:emu" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:trick">
<p>This example is simplified to illustrate the concept - a real implementation would handle state more carefully. <a class="footnote-backref" href="#fnref:trick" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/18-partial-inlining</id>
    <title>Partial inlining</title>

    <summary>Inlining doesn't have to be all-or-nothing</summary>

    <link href="http://xania.org/202512/18-partial-inlining?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-18T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>We've learned how important inlining is to optimisation, but also that it might sometimes cause code bloat. Inlining doesn't have to be all-or-nothing!</p>

<p>Let's look at a simple function that has a fast path and slow path; and then see how the compiler handles it<sup id="fnref:note"><a class="footnote-ref" href="#fn:note">1</a></sup>.</p>

<p>In this example we have some <code>process</code> function that has a really trivial fast case for numbers in the range 0-100. For other numbers it does something more expensive. Then <code>compute</code> calls <code>process</code> twice (making it less appealing to inline all of <code>process</code>).</p>

<p>Looking at the assembly output, we see what's happened: The compiler has split <code>process</code> into two functions, a <code>process (part.0)</code> that does the expensive part only. It then rewrites <code>process</code> into the quick check for 100, returning double the value if less than 100. If not, it jumps to the <code>(part.0)</code> function:</p>

<div class="codehilite"><pre><span /><code><span class="nf">process</span><span class="p">(</span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">):</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="mi">99</span><span class="w">                           </span><span class="c1">; less than or equal to 99?</span>
<span class="w">  </span><span class="nf">jbe</span><span class="w"> </span><span class="no">.L7</span><span class="w">                               </span><span class="c1">; skip to fast path if so</span>
<span class="w">  </span><span class="nf">jmp</span><span class="w"> </span><span class="no">process</span><span class="p">(</span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">)</span><span class="w"> </span><span class="p">(.</span><span class="no">part.0</span><span class="p">)</span><span class="w">   </span><span class="c1">; else jump to the expensive path</span>
<span class="nl">.L7:</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rdi</span><span class="p">]</span><span class="w">                    </span><span class="c1">; return `value * 2`</span>
<span class="w">  </span><span class="nf">ret</span>
</code></pre></div>


<p>This first step - extracting the cold path into a separate function - is called <strong>function outlining</strong>. The original <code>process</code> becomes a thin wrapper handling the hot path, delegating to the outlined <code>process (.part.0)</code> when needed. This split sets up the real trick: <strong>partial inlining</strong>. When the compiler later inlines <code>process</code> into <code>compute</code>, it inlines just the wrapper whilst keeping calls to the outlined cold path. External callers can still call <code>process</code> and have it work correctly for all values.</p>

<p>Let's see this optimisation in action in the <code>compute</code> function:</p>

<div class="codehilite"><pre><span /><code><span class="nf">compute</span><span class="p">(</span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">,</span><span class="w"> </span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">):</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="mi">99</span><span class="w">                   </span><span class="c1">; is a &lt;= 99?</span>
<span class="w">  </span><span class="nf">jbe</span><span class="w"> </span><span class="no">.L13</span><span class="w">                      </span><span class="c1">; if so, go to the inlined fast path for a</span>
<span class="w">  </span><span class="nf">call</span><span class="w"> </span><span class="no">process</span><span class="p">(</span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">)</span><span class="w"> </span><span class="p">(.</span><span class="no">part.0</span><span class="p">)</span><span class="w"> </span><span class="c1">; else, call expensive case</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">r8d</span><span class="p">,</span><span class="w"> </span><span class="no">eax</span><span class="w">                  </span><span class="c1">; save the result of process(a)</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">esi</span><span class="p">,</span><span class="w"> </span><span class="mi">99</span><span class="w">                   </span><span class="c1">; is b &lt;= 99?</span>
<span class="w">  </span><span class="nf">jbe</span><span class="w"> </span><span class="no">.L14</span><span class="w">                      </span><span class="c1">; if so go to the inlined fast path for b</span>
<span class="nl">.L11:</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="no">esi</span><span class="w">                  </span><span class="c1">; otherwise, call expensive case for b</span>
<span class="w">  </span><span class="nf">call</span><span class="w"> </span><span class="no">process</span><span class="p">(</span><span class="no">unsigned</span><span class="w"> </span><span class="no">int</span><span class="p">)</span><span class="w"> </span><span class="p">(.</span><span class="no">part.0</span><span class="p">)</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">r8d</span><span class="w">                  </span><span class="c1">; add the two slow cases together</span>
<span class="w">  </span><span class="nf">ret</span><span class="w">                           </span><span class="c1">; return</span>

<span class="nl">.L13:</span><span class="w">                           </span><span class="c1">; case where a is fast case</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">r8d</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="err">+</span><span class="no">rdi</span><span class="p">]</span><span class="w">            </span><span class="c1">; process(a) is just a + a</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">esi</span><span class="p">,</span><span class="w"> </span><span class="mi">99</span><span class="w">                   </span><span class="c1">; is b &gt; 99?</span>
<span class="w">  </span><span class="nf">ja</span><span class="w"> </span><span class="no">.L11</span><span class="w">                       </span><span class="c1">; jump to b slow case if so</span>
<span class="w">                                </span><span class="c1">; (falls through to...)</span>
<span class="nl">.L14:</span><span class="w">                           </span><span class="c1">; b fast case</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="err">+</span><span class="no">rsi</span><span class="p">]</span><span class="w">            </span><span class="c1">; double b</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">r8d</span><span class="w">                  </span><span class="c1">; return 2*a + 2*b</span>
<span class="w">  </span><span class="nf">ret</span>
</code></pre></div>


<p>Looking at <code>compute</code>, we can see the benefits of this approach clearly: The simple range check and arithmetic (<code>cmp</code>, <code>lea</code>) are inlined directly, avoiding the function call overhead for the fast path. When a value is 100 or greater, it calls the outlined <code>process (.part.0)</code> function for the more expensive computation.</p>

<p>This is the best of both worlds: we get the performance benefit of inlining the lightweight check and simple arithmetic, whilst avoiding code bloat from duplicating the expensive computation<sup id="fnref:again"><a class="footnote-ref" href="#fn:again">2</a></sup>. The original <code>process</code> function remains intact and callable, so external callers still work correctly.</p>

<p>Partial inlining lets the compiler make nuanced trade-offs about what to inline and what to keep shared. The compiler can outline portions of a function based on its heuristics about code size and performance<sup id="fnref:tradeoff"><a class="footnote-ref" href="#fn:tradeoff">3</a></sup>, giving you benefits of inlining without necessarily paying the full code size cost. In this example, the simple check is duplicated whilst the complex computation stays shared.</p>

<p>As with many optimisations, the compiler's heuristics<sup id="fnref:clang"><a class="footnote-ref" href="#fn:clang">4</a></sup> usually make reasonable choices about when to apply partial inlining, but it's worth checking your hot code paths to see if the compiler has made the decisions you expect. Taking a quick peek in <a href="https://godbolt.org">Compiler Explorer</a> is a good way to develop your intuition.</p>

<p><em>See <a href="https://youtu.be/STZb5K5sPDs">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 18 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/17-inlining-the-ultimate-optimisation">Inlining - the ultimate optimisation</a> | <a href="/202512/19-tail-call-optimisation">Chasing your tail</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:note">
<p>I have had to cheat a little here to get the output I want: I've actually disabled GCC's main inlining pass, otherwise it chooses to inline the whole of <code>process</code>. With a larger, more complex "slow path" that would be unnecessary, but in order to demonstrate the effect of partial inlining without generating tons of code, I'm using this slight cheat. <a class="footnote-backref" href="#fnref:note" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:again">
<p>Again, in this contrived example it probably <em>would</em> be OK to inline <code>process</code>, and the compiler really wants to, but for didactic purposes I've prevented that here. You can hopefully get the gist of this. <a class="footnote-backref" href="#fnref:again" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:tradeoff">
<p>Of course, nothing is free - duplicating code still takes up instruction cache space. The compiler's heuristics have to weigh the benefits against the costs, and different compilers make different choices. <a class="footnote-backref" href="#fnref:tradeoff" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:clang">
<p>Note that this varies substantially from compiler to compiler: I couldn't trick clang into making similar partial inlining decisions to gcc using flags, so I couldn't compare like with like. In my experience gcc and clang make quite different choices about inlining. <a class="footnote-backref" href="#fnref:clang" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/17-inlining-the-ultimate-optimisation</id>
    <title>Inlining - the ultimate optimisation</title>

    <summary>Copy paste can sometimes be a good thing, at least if the compiler does it for you</summary>

    <link href="http://xania.org/202512/17-inlining-the-ultimate-optimisation?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-17T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Sixteen days in, and I've been dancing around what many consider the fundamental compiler optimisation: inlining. Not because it's complicated - quite the opposite! - but because inlining is less interesting for what it does (copy-paste code), and more interesting for what it enables.</p>

<p>Initially inlining was all about avoiding the expense of the call<sup id="fnref:call"><a class="footnote-ref" href="#fn:call">1</a></sup> itself, but nowadays inlining enables many other optimisations to shine.</p>

<p>We've already encountered inlining (though I tried to limit it until now): On <a href="/202512/08-going-loopy">day 8</a> to get the size of a vector, we called its <code>.size()</code> method. I completely glossed over the fact that while <code>size()</code> is a method on <code>std::vector</code>, we don't see a call in the assembly code, just the subtraction and shift.</p>

<p>So, how does inlining enable other optimisations? Using ARMv7<sup id="fnref:arm"><a class="footnote-ref" href="#fn:arm">2</a></sup>, let's convert a string to uppercase. We might have a utility <code>change_case</code> function<sup id="fnref:contrived"><a class="footnote-ref" href="#fn:contrived">3</a></sup> that either turns a single character from upper to lower, or lower to upper, so, we'll use it in our code:</p>

<p>The compiler decides<sup id="fnref:force"><a class="footnote-ref" href="#fn:force">5</a></sup> to inline <code>change_case</code> into <code>make_upper</code>, and then seeing that <code>upper</code> is <em>always</em> <code>true</code><sup id="fnref:constprop"><a class="footnote-ref" href="#fn:constprop">4</a></sup>, it can simplify the whole code to:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.LBB0_1:</span>
<span class="w">  </span><span class="nf">ldrb</span><span class="w"> </span><span class="no">r2</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">r0</span><span class="p">]</span><span class="w">         </span><span class="c1">; read next `c`; c = *string;</span>
<span class="w">  </span><span class="nf">sub</span><span class="w"> </span><span class="no">r3</span><span class="p">,</span><span class="w"> </span><span class="no">r2</span><span class="p">,</span><span class="w"> </span><span class="mi">#97</span><span class="w">       </span><span class="c1">; tmp = c - 'a'</span>
<span class="w">  </span><span class="nf">uxtb</span><span class="w"> </span><span class="no">r3</span><span class="p">,</span><span class="w"> </span><span class="no">r3</span><span class="w">           </span><span class="c1">; tmp = tmp &amp; 0xff</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">r3</span><span class="p">,</span><span class="w"> </span><span class="mi">#26</span><span class="w">           </span><span class="c1">; check tmp against 26</span>
<span class="w">  </span><span class="nf">sublo</span><span class="w"> </span><span class="no">r2</span><span class="p">,</span><span class="w"> </span><span class="no">r2</span><span class="p">,</span><span class="w"> </span><span class="mi">#32</span><span class="w">     </span><span class="c1">; if lower than 26 then c = c - 32</span>
<span class="w">                        </span><span class="c1">; c = ((c - 'a') &amp; 0xff) &lt; 26 ? c - 32 : c;</span>
<span class="w">  </span><span class="nf">strb</span><span class="w"> </span><span class="no">r2</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">r0</span><span class="p">],</span><span class="w"> </span><span class="mi">#1</span><span class="w">     </span><span class="c1">; store 'c' back; *string++ = c</span>
<span class="w">  </span><span class="nf">subs</span><span class="w"> </span><span class="no">r1</span><span class="p">,</span><span class="w"> </span><span class="no">r1</span><span class="p">,</span><span class="w"> </span><span class="mi">#1</span><span class="w">       </span><span class="c1">; reduce counter</span>
<span class="w">  </span><span class="nf">bne</span><span class="w"> </span><span class="no">.LBB0_1</span><span class="w">           </span><span class="c1">; loop if not zero</span>
</code></pre></div>


<p>There's no trace left of the <code>!upper</code> case and the compiler, having inlined the code, has a fresh copy of the code to then further modify to take advantage of things it knows are true. It does a neat trick of avoiding a branch to check whether the character is uppercase: If <code>(c - 'a') &amp; 0xff</code><sup id="fnref:uxtb"><a class="footnote-ref" href="#fn:uxtb">6</a></sup> is less than 26, it must be a lowercase character. It then conditionally subtracts 32, which has the effect of making <code>a</code> into <code>A</code>.</p>

<p>Inlining gives the compiler the ability to make local changes: The implementation can be special cased at the inline site as by definition there's no other callers to the code. The special casing can include propagating values known to be constants (like the <code>upper</code> bool above), and looking for code paths that are unused<sup id="fnref:dce"><a class="footnote-ref" href="#fn:dce">7</a></sup>.</p>

<p>Inlining has some drawbacks though: if it's overused, the code size of your program can grow quite substantially<sup id="fnref:cache"><a class="footnote-ref" href="#fn:cache">8</a></sup>. The compiler has to make its best guess as to whether inlining a function (and the functions that <em>it</em> calls...and so on), based on heuristics about the code size increase, and whether the perceived benefit is worth it. Ultimately it's a guess though.</p>

<p>In rare cases accepting the cost of calling a common routine <em>can</em> be a benefit: if there is an unavoidable branch in the routine that's globally predictable, sometimes having one shared branch site can be better for the branch predictor. In many cases, though, the reverse is true: if there's a branch in code that's inlined many times across the codebase then sometimes the (more local) branch history for the many copies of that branch can yield more predictability. It's...complex<sup id="fnref:nuanced"><a class="footnote-ref" href="#fn:nuanced">9</a></sup>.</p>

<p>An important consideration for inlining is the visibility of the <em>definition</em> of the function you're calling (that is, the body of the function). If the compiler has only seen the <em>declaration</em> of a function (e.g. in the case above just <code>char change_case(char c, bool upper);</code>), then it can't inline it: there's nothing to inline! In modern C++ with templates and a lot of code in headers, this usually isn't a problem, but if you're trying to minimise build times and interdependency this can be an issue<sup id="fnref:lto"><a class="footnote-ref" href="#fn:lto">10</a></sup>.</p>

<p>Frustratingly, inlining is also one of the most heuristic-driven optimisations; with different compilers making reasonable but different guesses about which functions should be inlined. This can be frustrating when adding a single line to a function somewhere has ripple effects throughout a codebase affecting inlining decisions<sup id="fnref:amit"><a class="footnote-ref" href="#fn:amit">11</a></sup>.</p>

<p>All that said: Inlining is the ultimate enabling optimisation. On its own, copying function bodies into call sites might save a few cycles here and there. But give the compiler a fresh copy of code at the call site, and suddenly it can propagate constants, eliminate dead branches, and apply transformations that would be impossible with a shared function body. Who said copy paste was always bad?</p>

<p><em>See <a href="https://youtu.be/JFHfFTvMPp0">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 17 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/16-calling-conventions">Calling all arguments</a> | <a href="/202512/18-partial-inlining">Partial inlining</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:call">
<p>The <code>call</code> and <code>return</code> itself, coupled with the preserving and restoring of registers required for the calling convention. <a class="footnote-backref" href="#fnref:call" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:arm">
<p>Again, this is for simplicity and to avoid vectorisation which, while super important, is something we'll get to later. <a class="footnote-backref" href="#fnref:arm" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:contrived">
<p>A bit contrived, and I'm deliberately rolling my own <code>std::toupper</code> etc to avoid locales, and further function calls. <a class="footnote-backref" href="#fnref:contrived" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:constprop">
<p>This is called constant propagation - when the compiler knows a value is constant, it substitutes it everywhere and simplifies the result. I had planned to do a post on this alone, but somehow I didn't have space for it! <a class="footnote-backref" href="#fnref:constprop" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:force">
<p>I deliberately made the <code>change_case</code> function <code>static</code> so it won't even appear in the output here as it's otherwise unused. This also strongly hints to the compiler's optimiser to inline it. If I made it non-static, then <code>make_upper</code> doesn't change at all (it's still inlined), but there's a big (unused) <code>change_case</code> in the output to confuse things. Give it a go and look at the more complex code generated in <code>change_case</code>! <a class="footnote-backref" href="#fnref:force" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:uxtb">
<p>The <code>&amp; 0xff</code> comes from the "Unsigned eXTend Byte" <code>uxtb</code> instruction. <a class="footnote-backref" href="#fnref:uxtb" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
<li id="fn:dce">
<p>Also known as dead code elimination, if the compiler can prove that parts of the code are unreachable it can remove them. <a class="footnote-backref" href="#fnref:dce" title="Jump back to footnote 7 in the text">↩</a></p>
</li>
<li id="fn:cache">
<p>This can have performance effects: the instruction cache is a limited resource so filling it up with lots of copies of essentially the same code can put extra pressure on the memory and decode subsystem of the CPU. <a class="footnote-backref" href="#fnref:cache" title="Jump back to footnote 8 in the text">↩</a></p>
</li>
<li id="fn:nuanced">
<p>That is, it's a nuanced trade-off that depends on the specific code and runtime patterns. The compiler often doesn't have that kind of information about your code, and so has to guess. Things like Profile Guided Optimisation can help this, but we won't be covering that in this series. <a class="footnote-backref" href="#fnref:nuanced" title="Jump back to footnote 9 in the text">↩</a></p>
</li>
<li id="fn:lto">
<p>Using link-time optimisation (LTO; sometimes called link-time code generation), will allow the compiler to inline across translation units. LTO is a powerful technique and is well-supported on compilers. I always enable LTO for my release builds. <a class="footnote-backref" href="#fnref:lto" title="Jump back to footnote 10 in the text">↩</a></p>
</li>
<li id="fn:amit">
<p>Quite famously, once a single newline in a function definition caused a <a href="https://sites.google.com/site/nadavamit/blog/linux-inline">significant Linux performance regression</a>, though this is more a limitation with the way GCC chooses to interpret inline assembly's cost during inline analysis. <a class="footnote-backref" href="#fnref:amit" title="Jump back to footnote 11 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/16-calling-conventions</id>
    <title>Calling all arguments</title>

    <summary>Knowing how compilers call functions can help with design - and optimisation</summary>

    <link href="http://xania.org/202512/16-calling-conventions?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-16T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Today we're looking at calling conventions<sup id="fnref:jason"><a class="footnote-ref" href="#fn:jason">1</a></sup> - which aren't purely optimisation related but are important to understand. The calling convention is part of the ABI (Application Binary Interface), and varies from architecture to architecture and even OS to OS. Today I'll concentrate on the System V ABI for x86 on Linux, as (to me) it's the most sane ABI.</p>

<p>Before we go on: I'd be remiss if I didn't point out that I can <em>never</em> remember which register has what in it, and for years I had a Post It note on my monitor with a hand-written crib sheet of the ABI. While on holiday I had an idea: Why not put the ABI on a mug! I created these ABI mugs and you can get your own one - and support Compiler Explorer - at the <a href="https://shop.compiler-explorer.com/">Compiler Explorer shop</a>.</p>

<p class="picture">
<a href="https://shop.compiler-explorer.com">
<img src="16-mugs.jpeg" alt="Some Compiler Explorer mugs with ABI information on them" width="655" height="330" />
</a>
<br />The Compiler Explorer ABI mug: Get yours at the <a href="https://shop.compiler-explorer.com">CE Shop!</a>
</p>


<p>We've already touched on calling conventions as I've commented the assembly in previous days, but concretely, for x86 Linux, the first couple of parameters go in <code>rdi</code> and <code>rsi</code><sup id="fnref:smaller"><a class="footnote-ref" href="#fn:smaller">2</a></sup>. This makes sense for discrete integer types, and even pointers. What about structures? Let's compare two functions:</p>

<p>With <code>ArgType</code> set to <code>long</code> it might surprise you that the body of those two functions are identical! Both are just <code>lea rax, [rdi+rsi]</code>: We expect the separate arguments to be in <code>rdi</code> and <code>rsi</code>, but passing that larger structure by value <em>also</em> got placed in <code>rdi</code> and <code>rsi</code>. Neat!</p>

<p>If you explore by changing the <code>ArgType</code> you'll see that the compiler has to do more work if we don't (conveniently) use <code>long</code>s here. For example, changing to <code>int</code> changes the separate argument version to <code>lea eax...</code> to reflect the 32-bit return value, but the body of the structure version becomes:</p>

<div class="codehilite"><pre><span /><code><span class="c1">; structure is _packed_ into rdi as "y&lt;&lt;32 | x"</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rdi</span><span class="w">      </span><span class="c1">; rax = args.y&lt;&lt;32 | args.x</span>
<span class="w">  </span><span class="nf">shr</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="mi">32</span><span class="w">       </span><span class="c1">; rax &gt;&gt;= 32; rax is now 'y'</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">edi</span><span class="w">      </span><span class="c1">; y += x;</span>
<span class="w">  </span><span class="nf">ret</span>
</code></pre></div>


<p>It's a little tricky to follow as the compiler is cunningly switching between the 64-bit <code>r</code> prefixed register names and the 32-bit <code>e</code> versions, but you can see that, for the cost of a couple more instructions we still passed the structure pretty efficiently, and in a single register.</p>

<p>It gets more interesting when we pass <em>lots</em> of arguments<sup id="fnref:dont"><a class="footnote-ref" href="#fn:dont">3</a></sup>. Even on System V ABI, only the first 6 parameters are passed in registers. After that, it spills to the stack. Let's update our example to pass many arguments to show this:</p>

<p>In this case we can see the separate args version adding all the registers, and then having to get some off the stack: <code>add rax, QWORD PTR [rsp+8]</code> and so on. On the structure side, the whole structure<sup id="fnref:value"><a class="footnote-ref" href="#fn:value">5</a></sup> is copied to the stack, which seems bad<sup id="fnref:vec"><a class="footnote-ref" href="#fn:vec">4</a></sup>.</p>

<p>However! Changing <code>ArgType</code> to <code>char</code> and you'll see an interesting difference. The separate args version is similar, though it can't use <code>lea</code> any more, and has to sign-extend all the values its getting from its registers. The struct args version has no stack spillage: our entire <code>StructArgs</code> structure fits into one register! The compiler has a bit of a time shifting it around to extract each <code>char</code>, but it's not having to spill to the stack at least. In my testing, different compilers used different tricks, so play around with the <code>ArgType</code> and compiler (e.g. clang) and get a sense of what it can do<sup id="fnref:explain"><a class="footnote-ref" href="#fn:explain">6</a></sup>.</p>

<p>So, all this is pretty abstract: why is this important? Sometimes knowing the ABI, and how the compiler can optimise around it can inform your design. There was some debate about the design of <code>std::string_view</code> and <code>std::optional</code> and their usefulness<sup id="fnref:windows"><a class="footnote-ref" href="#fn:windows">8</a></sup>: these are types that are convenient to pass <em>by value</em>, and so their footprint in registers is important<sup id="fnref:svlayout"><a class="footnote-ref" href="#fn:svlayout">7</a></sup>.</p>

<p>Overall, knowing the calling convention helps you make smart decisions about layout and parameter passing that give the compiler the best shot at generating efficient code.</p>

<p><em>See <a href="https://youtu.be/Yaw8AMoP4sI">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 16 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/15-aliasing-in-general">Aliasing</a> | <a href="/202512/17-inlining-the-ultimate-optimisation">Inlining - the ultimate optimisation</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:jason">
<p>Big thanks to <a href="https://articles.emptycrate.com/">Jason Turner</a> for suggesting this topic and providing some fun examples. <a class="footnote-backref" href="#fnref:jason" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:smaller">
<p>Or their smaller siblings <code>edi</code>/<code>esi</code>, <code>di</code>/<code>si</code> etc <a class="footnote-backref" href="#fnref:smaller" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:dont">
<p>Which in general is a <em>bad</em> idea anyway. Don't pass lots of arguments, use structures and well defined types, not a load of bools and ints and whatnot. <a class="footnote-backref" href="#fnref:dont" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:vec">
<p>Though the compiler does a decent job of using vector operations here. Which I promise we'll talk about eventually! <a class="footnote-backref" href="#fnref:vec" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:value">
<p>Remember we're passing by <em>value</em> here, and typically you might normally pass by const reference. Experiment with the code and see the difference it makes. <a class="footnote-backref" href="#fnref:value" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:explain">
<p>If you get confused, pop out the view with the "Edit on Compiler Explorer" button and consider trying the Claude Explain AI explanation panel from the "Add new..." drop down. <a class="footnote-backref" href="#fnref:explain" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
<li id="fn:svlayout">
<p>A <code>std::string_view</code> is essentially a pointer and a length - two values that fit neatly into two registers, just like the two-<code>long</code> struct example above. <a class="footnote-backref" href="#fnref:svlayout" title="Jump back to footnote 7 in the text">↩</a></p>
</li>
<li id="fn:windows">
<p>The Windows ABI in particular only has four register arguments and so only two <code>string_view</code>s can be passed before things end up on the stack. <a class="footnote-backref" href="#fnref:windows" title="Jump back to footnote 8 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/15-aliasing-in-general</id>
    <title>Aliasing</title>

    <summary>Knowing when the compiler can't optimise is important too</summary>

    <link href="http://xania.org/202512/15-aliasing-in-general?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-15T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p><a href="/202512/14-licm-when-it-doesnt">Yesterday</a> we ended on a bit of a downer: aliasing stopped optimisations dead in their tracks. I know this is supposed to be the <em>Advent of Compiler Optimisations</em>, not the <em>Advent of Compiler Giving Up</em>! Knowing why your compiler can't optimise is just as important as knowing all the clever tricks it can pull off.</p>

<p>Let's take a simple example of a counter class<sup id="fnref:actually"><a class="footnote-ref" href="#fn:actually">1</a></sup>. It accumulates integers into a member variable <code>total</code>. I've used C++ templates to show two versions of the code: one that accumulates in an <code>int</code> and one that accumulates in an <code>long</code>.</p>

<p>At first glance, the two loop bodies look almost identical, as you might expect. In one case we'll accumulate in <code>eax</code>, and the other in <code>rax</code>, right? The truth is more subtle. Let's first look at the <code>int</code> case:</p>

<div class="codehilite"><pre><span /><code><span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="p">]</span><span class="w">      </span><span class="c1">; eax = total</span>
<span class="nl">.L3:</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="p">]</span><span class="w">      </span><span class="c1">; add element to total</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rsi</span><span class="p">,</span><span class="w"> </span><span class="mi">4</span><span class="w">                    </span><span class="c1">; move to next element</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="p">],</span><span class="w"> </span><span class="no">eax</span><span class="w">      </span><span class="c1">; total = eax</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rsi</span><span class="p">,</span><span class="w"> </span><span class="no">rdx</span><span class="w">                  </span><span class="c1">; are we finished?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L3</span><span class="w">                       </span><span class="c1">; loop if not</span>
<span class="w">  </span><span class="nf">ret</span>
</code></pre></div>


<p>Looks pretty reasonable, right? Now let's look at the <code>long</code> version:</p>

<div class="codehilite"><pre><span /><code><span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">QWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="p">]</span><span class="w">      </span><span class="c1">; rax = total</span>
<span class="nl">.L9:</span>
<span class="w">  </span><span class="nf">movsx</span><span class="w"> </span><span class="no">rdx</span><span class="p">,</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rsi</span><span class="p">]</span><span class="w">    </span><span class="c1">; read &amp; sign extend next element</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rsi</span><span class="p">,</span><span class="w"> </span><span class="mi">4</span><span class="w">                    </span><span class="c1">; move to next element</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rdx</span><span class="w">                  </span><span class="c1">; rax += element</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rcx</span><span class="p">,</span><span class="w"> </span><span class="no">rsi</span><span class="w">                  </span><span class="c1">; are we finished?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L9</span><span class="w">                       </span><span class="c1">; loop if not</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">QWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi</span><span class="p">],</span><span class="w"> </span><span class="no">rax</span><span class="w">      </span><span class="c1">; total = rax</span>
<span class="w">  </span><span class="nf">ret</span><span class="w">                           </span><span class="c1">; return</span>
</code></pre></div>


<p>The first change from the 32-bit case you'll notice is the <code>movsx</code> to turn the 32-bit signed integer into a 64-bit signed integer. That's all but free on modern CPUs, and so while it looks like the loop is doing more work than the 32-bit version, it's not as bad as it seems.</p>

<p>The important difference here is the update of <code>total</code>: In the first version, each loop iteration updates the member variable <code>total</code>. In the second version everything remains in registers until the end of the loop, and then <code>total</code> is only updated at the end. CPU caches are super fast, but it's still best to avoid redundant stores in hot loops!</p>

<p>So, why this difference? Of course it's aliasing: In the <code>int</code> version the compiler can't be sure that the <code>span</code> passed in to <code>count</code> doesn't cover the <code>Counter</code>'s member variable <code>total</code>. They are the same type, and so that would be perfectly OK by the type-based aliasing rules of C++.</p>

<p>In the <code>long</code> version, the types differ (<code>int</code> vs <code>long</code>), and under C++'s strict aliasing rules, it would be undefined behaviour for them to overlap in memory. Since the compiler can assume the program doesn't invoke undefined behaviour, it knows they don't alias and can safely optimise. That lets it cache the <code>total</code> in a register and only update the member variable at the end. As we'll see later in the series, being aliasing-free helps other optimisations too, like vectorisation.</p>

<p>To fix this issue, we have a couple of choices. The easy way would be to rewrite to accumulate in a local variable, and then update the total at the end: Using <code>total += std::accumulate(span.begin(), span.end(), 0)</code> would fix this <em>and</em> be more intention-revealing.</p>

<p>The other, non-standard way to work around this issue is to use <code>__restrict</code>. This GNU extension (borrowed from C) lets us decorate pointers and essentially promises that this pointer uniquely refers to the object it points at. In our case, the thing we need to prove is unique is the <code>Counter</code>'s "this pointer" itself. Adding <code>__restrict</code> after the parameter list (where you would add <code>const</code> for a const member function) works. But again - this is very non-standard, so use at your peril<sup id="fnref:vectorise"><a class="footnote-ref" href="#fn:vectorise">2</a></sup>.</p>

<p>Aliasing is one of C++'s sneakier gotchas, especially when you're working with base types like <code>int</code> and <code>float</code> - you can't avoid using them, and they're prime candidates for aliasing with each other<sup id="fnref:rust"><a class="footnote-ref" href="#fn:rust">3</a></sup>. It's perfectly legal to have overlapping same-type pointers, so the compiler assumes the worst and peppers your hot loops with memory accesses. The fix may be as simple as only using local variables within your loop - but first you have to spot it. Fire up <a href="https://godbolt.org">Compiler Explorer</a>, look for those unexpected writes to memory when you'd expect everything to stay in registers, and you'll know when aliasing is holding you back!</p>

<p><em>See <a href="https://youtu.be/PPJtJzT2U04">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 15 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/14-licm-when-it-doesnt">When LICM fails us</a> | <a href="/202512/16-calling-conventions">Calling all arguments</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:actually">
<p>This example is taken from something similar that actually bit me in my job a few years ago. I was bewildered until I worked out that it was aliasing! <a class="footnote-backref" href="#fnref:actually" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:vectorise">
<p>Try making the change in the Compiler Explorer window above. You'll see the <code>total</code> update move outside of the loop. If you're not scared to jump ahead a bit, make it <code>-O3</code> and you'll see the vectorised code, but only with <code>__restrict</code>. <a class="footnote-backref" href="#fnref:vectorise" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:rust">
<p>Aliasing is particularly bad in C and C++. In other languages like Rust and Fortran, aliasing can't happen at all, or is severely restricted. This can help their optimisers considerably. <a class="footnote-backref" href="#fnref:rust" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/14-licm-when-it-doesnt</id>
    <title>When LICM fails us</title>

    <summary>When aliasing can prevent loop-invariant code motion</summary>

    <link href="http://xania.org/202512/14-licm-when-it-doesnt?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-14T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p><a href="/202512/13-licking-licm">Yesterday's LICM post</a> ended with the compiler pulling invariants like <code>size()</code> and <code>get_range</code> out of our loop - clean assembly, great performance. Job done, right?</p>

<p>Not quite. Let's see how that optimisation can disappear.</p>

<p>Let's say you had a <code>const char *</code><sup id="fnref:cpp"><a class="footnote-ref" href="#fn:cpp">1</a></sup>, and wanted to write a function to return if there was an exclamation mark or not:</p>

<p>Here we're relying on loop-invariant code motion (LICM) to move the <code>strlen</code> out of the loop, called once before we loop, and then the loop is:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L4:</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="mi">1</span><span class="w">                </span><span class="c1">; ++index</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">BYTE</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-1</span><span class="p">],</span><span class="w"> </span><span class="mi">33</span><span class="w">  </span><span class="c1">; is string[index-1] a '!' ?</span>
<span class="w">  </span><span class="nf">je</span><span class="w"> </span><span class="no">.L5</span><span class="w">                    </span><span class="c1">; if so, jump to "return true"</span>
<span class="nl">.L2:</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="no">rax</span><span class="w">              </span><span class="c1">; else...are we at the end?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L4</span><span class="w">                   </span><span class="c1">; loop if not</span>
</code></pre></div>


<p>All pretty good. We'll learn in later posts<sup id="fnref:vector"><a class="footnote-ref" href="#fn:vector">2</a></sup> that we can improve on this simple loop, but for now this seems ok.</p>

<p>Now let's assume we were curious how many characters in total we were comparing across our entire program, and add some basic instrumentation<sup id="fnref:ig"><a class="footnote-ref" href="#fn:ig">3</a></sup>:</p>

<p>We might reasonably expect a single increment to be added to our loop. But if we take a closer look:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L4:</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">QWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="no">num_compares</span><span class="p">[</span><span class="no">rip</span><span class="p">],</span><span class="w"> </span><span class="mi">1</span><span class="c1">; ++num_compares;</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">BYTE</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rbp</span><span class="err">+</span><span class="mi">0</span><span class="err">+</span><span class="no">rbx</span><span class="p">],</span><span class="w"> </span><span class="mi">33</span><span class="w">      </span><span class="c1">; is char a '!' ?</span>
<span class="w">  </span><span class="nf">je</span><span class="w"> </span><span class="no">.L5</span><span class="w">                            </span><span class="c1">; if so, jump to "return true"</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rbx</span><span class="p">,</span><span class="w"> </span><span class="mi">1</span><span class="w">                        </span><span class="c1">; ++index</span>
<span class="nl">.L2:</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="no">rbp</span><span class="w">                      </span><span class="c1">; er...</span>
<span class="w">  </span><span class="nf">call</span><span class="w"> </span><span class="no">strlen</span><span class="w">                       </span><span class="c1">; what the heck? strlen?</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rbx</span><span class="p">,</span><span class="w"> </span><span class="no">rax</span><span class="w">                      </span><span class="c1">; oh no! what happened?</span>
<span class="w">  </span><span class="nf">jb</span><span class="w"> </span><span class="no">.L4</span><span class="w">                            </span><span class="c1">; loop if index != strlen(...)</span>
</code></pre></div>


<p>Suddenly we've lost our lovely LICM! By simply incrementing a global variable<sup id="fnref:clang"><a class="footnote-ref" href="#fn:clang">4</a></sup>, we lost the ability to call <code>strlen</code> and keep the result. Why is this?</p>

<p>It comes down to aliasing: the compiler can't prove that the string we're getting the length of doesn't share memory with the <code>num_compares</code> variable! Every time we modify <code>num_compares</code> the compiler has to assume that <em>maybe</em> the string changed as a result!</p>

<p>This seems pretty odd: why on earth would a string overlap with a <code>std::size_t</code>? Don't we have rules about this? Aren't we disallowed from doing such things - type punning is barred - at least in C++?</p>

<p>Unfortunately, <code>char*</code> has a <strong>special</strong> status in the standard<sup id="fnref:aliasing"><a class="footnote-ref" href="#fn:aliasing">5</a></sup>: it's allowed to alias with <strong>anything</strong>. That's why the compiler can't assume our string and <code>num_compares</code> occupy different memory! If we had any other type than <code>char *</code> then the compiler <em>could</em> use LICM.</p>

<p>At least, that's what I hoped. In practice neither GCC<sup id="fnref:gcc-bug"><a class="footnote-ref" href="#fn:gcc-bug">6</a></sup>, clang<sup id="fnref:clang-bug"><a class="footnote-ref" href="#fn:clang-bug">7</a></sup> nor MSVC were able to LICM the code I tried. I could easily be missing something here, so do let me know if you have any ideas.</p>

<p>Today's post seems a bit of a downer: the compiler wasn't able to do an optimisation I had otherwise hoped it might. Tomorrow we'll look at some other examples where the compiler struggles with aliasing, and we'll see how to help it out.</p>

<p><em>See <a href="https://youtu.be/OwFNblEEAXo">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 14 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/13-licking-licm">Loop-Invariant Code Motion</a> | <a href="/202512/15-aliasing-in-general">Aliasing</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:cpp">
<p>Think of this as more a C-style implementation: in C++ we'd probably use a <code>string_view</code> here which avoids the specific issue we're about to run in to. And of course <code>strchr</code> and friends are more optimal, but...bear with me! <a class="footnote-backref" href="#fnref:cpp" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:vector">
<p>Specifically, either by using <code>strchr</code> or other settings, we can get the compiler's help to vectorize this. We'll talk more of that later. <a class="footnote-backref" href="#fnref:vector" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:ig">
<p>We're going to ignore any threading issues here. <a class="footnote-backref" href="#fnref:ig" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:clang">
<p>Making the variable <code>static</code> helps for clang, but not gcc. Clang sees that we don't use the variable and gets rid of it, but if we were to use it elsewhere it's possible it wouldn't be able to do this optimisation. <a class="footnote-backref" href="#fnref:clang" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:aliasing">
<p>See <a href="https://eel.is/c++draft/basic.lval#11">[basic.lval] paragraph 11</a> in the C++ draft standard, which lists <code>char</code>, <code>unsigned char</code>, and <code>std::byte</code> as exceptions to the strict aliasing rule. <a class="footnote-backref" href="#fnref:aliasing" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:gcc-bug">
<p>I filed a <a href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=122240">missed optimisation opportunity</a> for GCC and the thought is the heuristic that handles inlining is perhaps making a poor choice. We'll talk about inlining later. <a class="footnote-backref" href="#fnref:gcc-bug" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
<li id="fn:clang-bug">
<p>Clang's optimiser had slightly different characteristics, and I <a href="https://github.com/llvm/llvm-project/issues/163097">filed this issue</a> to track. <a class="footnote-backref" href="#fnref:clang-bug" title="Jump back to footnote 7 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/13-licking-licm</id>
    <title>Loop-Invariant Code Motion</title>

    <summary>The compiler can move code outside of loops to speed things up</summary>

    <link href="http://xania.org/202512/13-licking-licm?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-13T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Look back at <a href="/202512/08-going-loopy">our simple loop example</a> - there's an optimisation I completely glossed over. Let me show you what I mean:</p>

<p>On every loop iteration we are calling <code>vec.size()</code> to compare the index value, and to check if the index has reached the end of the vector. However, looking in the assembly, the compiler has pulled the size calculation<sup id="fnref:recall"><a class="footnote-ref" href="#fn:recall">1</a></sup> out of the loop entirely - the <code>sar</code> that divides the "end - start" value by the size of an int is only performed <em>before</em> the loop! The compiler quietly rewrote our code to be:</p>

<p>Such a transformation is called Loop-Invariant Code Motion, or LICM. It's not just expressions in the <code>for</code> clauses themselves, any code inside the loop that the compiler can prove doesn't depend on which iteration it's in is fair game.</p>

<p>Let's give our loop something more to do: We'll take a <code>std::string_view</code><sup id="fnref:sv"><a class="footnote-ref" href="#fn:sv">2</a></sup> and count how many characters fall within a given range (being "capital letters" or "numerics")<sup id="fnref:const"><a class="footnote-ref" href="#fn:const">3</a></sup>. We'll write it naively, using a <code>get_range()</code> function that returns the min and max character "in range" as a pair:</p>

<p>You can see that clang<sup id="fnref:gcc"><a class="footnote-ref" href="#fn:gcc">4</a></sup> here has realised the range from <code>get_range</code> cannot change during the loop and so has moved it outside:</p>

<div class="codehilite"><pre><span /><code><span class="nl">count_in_range:</span>
<span class="c1">; ...preamble removed for brevity...</span>
<span class="w">  </span><span class="nf">call</span><span class="w"> </span><span class="no">get_range</span><span class="p">(</span><span class="no">RangeType</span><span class="p">)</span><span class="err">@</span><span class="no">PLT</span><span class="w">     </span><span class="c1">; call get_range (OUTSIDE OF LOOP)</span>
<span class="w">  </span><span class="nf">movsx</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="no">al</span><span class="w">                     </span><span class="c1">; ecx = range.first</span>
<span class="w">  </span><span class="nf">shr</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="mi">8</span>
<span class="w">  </span><span class="nf">movsx</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">al</span><span class="w">                     </span><span class="c1">; edx = range.second</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">esi</span><span class="p">,</span><span class="w"> </span><span class="no">esi</span><span class="w">                      </span><span class="c1">; esi = loop counter = 0</span>
<span class="w">  </span><span class="nf">xor</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">eax</span><span class="w">                      </span><span class="c1">; eax = num = 0</span>
<span class="nl">.LBB0_4:</span>
<span class="w">  </span><span class="nf">movsx</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="no">byte</span><span class="w"> </span><span class="no">ptr</span><span class="w"> </span><span class="p">[</span><span class="no">rbx</span><span class="w"> </span><span class="err">+</span><span class="w"> </span><span class="no">rsi</span><span class="p">]</span><span class="w">   </span><span class="c1">; read next c</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">ecx</span><span class="p">,</span><span class="w"> </span><span class="no">edi</span><span class="w">                      </span><span class="c1">; compare with first</span>
<span class="w">  </span><span class="nf">setle</span><span class="w"> </span><span class="no">r8b</span><span class="w">                         </span><span class="c1">; r8b = c &gt;= first</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="no">edi</span><span class="w">                      </span><span class="c1">; compare with second</span>
<span class="w">  </span><span class="nf">setge</span><span class="w"> </span><span class="no">dil</span><span class="w">                         </span><span class="c1">; dil = c &lt;= second</span>
<span class="w">  </span><span class="nf">and</span><span class="w"> </span><span class="no">dil</span><span class="p">,</span><span class="w"> </span><span class="no">r8b</span><span class="w">                      </span><span class="c1">; dil = dil &amp; r8b</span>
<span class="w">                                    </span><span class="c1">; ie 1 if c in range, else 0</span>
<span class="w">  </span><span class="nf">movzx</span><span class="w"> </span><span class="no">edi</span><span class="p">,</span><span class="w"> </span><span class="no">dil</span><span class="w">                    </span><span class="c1">; edi = (zero extended) dil</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="no">rdi</span><span class="w">                      </span><span class="c1">; num += (1 if in range, else 0)</span>
<span class="w">  </span><span class="nf">inc</span><span class="w"> </span><span class="no">rsi</span><span class="w">                           </span><span class="c1">; increment loop counter</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">r14</span><span class="p">,</span><span class="w"> </span><span class="no">rsi</span><span class="w">                      </span><span class="c1">; are we done?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.LBB0_4</span><span class="w">                       </span><span class="c1">; loop if not</span>
<span class="c1">; ...postamble/return removed for brevity...</span>
</code></pre></div>


<p>Clang has also played some other neat tricks<sup id="fnref:ahem"><a class="footnote-ref" href="#fn:ahem">5</a></sup> here to avoid a branch inside the loop using <code>setle</code> and <code>setge</code> to get a 0 or 1 based on a condition code.</p>

<p>Usually I end with a "trust the compiler" type sentiment. However, in this case I was surprised that gcc wasn't able to perform code motion here<sup id="fnref:bug"><a class="footnote-ref" href="#fn:bug">6</a></sup>. I guess that's what <a href="https://godbolt.org">Compiler Explorer</a> is for: Trust the compiler, but know how to verify its output too.</p>

<p><em>See <a href="https://youtu.be/dIwaqJG0WDo">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 13 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/12-loop-unswitching">Unswitching loops for fun and profit</a> | <a href="/202512/14-licm-when-it-doesnt">When LICM fails us</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:recall">
<p>Recall that the vector holds various pointers, and to determine the count of elements, we need to subtract the <code>end</code> from the <code>start</code> and divide by the element size. <a class="footnote-backref" href="#fnref:recall" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:sv">
<p>A <code>std::string_view</code> is, in this case, equivalent to <code>std::span&lt;char&gt;</code>. <a class="footnote-backref" href="#fnref:sv" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:const">
<p>I haven't actually supplied the body of the <code>get_range</code> function here - it would add more code <em>and</em> would show the compiler doing inlining, which I'll cover properly later in this series. However, I do need to use a magic attribute <code>[[gnu::pure]]</code> here to let the compiler know that the implementation of this function is "pure" and depends only on its inputs. The compiler usually figures this out for itself from the implementation if it can see it. <a class="footnote-backref" href="#fnref:const" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:gcc">
<p>Surprisingly, gcc seems unable to take advantage of this optimisation and in fact calls <code>get_range</code> <em>twice</em> each iteration, even if I use the even-stricter <code>[[gnu::const]]</code> attribute. <a class="footnote-backref" href="#fnref:gcc" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:ahem">
<p>I've deliberately used <code>-O1</code> only here to minimise some of the <em>even more clever</em> tricks clang will do here. I will cover some of those later in this series. <a class="footnote-backref" href="#fnref:ahem" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:bug">
<p>After speaking with some other C++ experts, I filed a <a href="https://gcc.gnu.org/bugzilla/show_bug.cgi?id=122226">gcc bug</a> on a similar but largely equivalent example. A gcc maintainer suggests it's due to the structure type returned (a <code>std::pair</code> here) doesn't go through common subexpression elimination (CSE<sup id="fnref:CSE"><a class="footnote-ref" href="#fn:CSE">7</a></sup>), which prevents the analysis needed to do LICM. <a class="footnote-backref" href="#fnref:bug" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
<li id="fn:CSE">
<p>We won't cover CSE in this series, but the compiler tries to detect redundant calculation of the same subexpressions, and only does the work once where possible. <a class="footnote-backref" href="#fnref:CSE" title="Jump back to footnote 7 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/12-loop-unswitching</id>
    <title>Unswitching loops for fun and profit</title>

    <summary>Duplicating loops around can yield some decent optimisations</summary>

    <link href="http://xania.org/202512/12-loop-unswitching?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-12T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Sometimes the compiler decides the best way to optimise your loop is to... write it twice. Sounds counterintuitive? Let's change our <code>sum</code> <a href="/202512/10-loop-unrolling">example from before</a> to optionally return a sum-of-squares<sup id="fnref:armagain"><a class="footnote-ref" href="#fn:armagain">1</a></sup>:</p>

<p>At <code>-O2</code> the compiler turns the ternary into: <code>sum += value * (squared ? value : 1);</code> - using a multiply and add (<code>mla</code>) instruction to do the multiply and add, and conditionally picking either <code>value</code> or the constant <code>1</code> to avoid a branch inside the loop.</p>

<p>However, if we turn the optimisation level up, the compiler uses a new approach:</p>

<p>Here the compiler realises the <code>bool squared</code> value doesn't change throughout the loop, and decides to duplicate the loop: one copy that squares each time unconditionally, and one where it never multiplies at all. This is called "loop unswitching".</p>

<p>The check of <code>squared</code> is moved out of the loop, and the appropriate loop is then selected, either <code>.LBB0_4</code> (non-squaring) or continuing to <code>.LBB0_2</code> (the squaring version).</p>

<p>Each loop is perfectly optimised for its duties, and it's as if you had written:</p>

<p>By duplicating the loop this way, the compiler makes sure that the multiplication doesn't happen unless you specifically asked for it. In the <code>-O2</code> code, the compiler bet on doing the multiply-and-add each time<sup id="fnref:bpu"><a class="footnote-ref" href="#fn:bpu">2</a></sup> even when it wasn't needed. In the loop unswitching case, we do pay a code size penalty (<em>some</em> code is duplicated, after all), and that's why loop unswitching didn't occur on the lower optimisation setting.</p>

<p>As always, it's good to trust your compiler's decisions, but know the kinds of trade-offs it's making at various optimisation levels. You can always verify what it's doing with <a href="https://godbolt.org">Compiler Explorer</a>, after all!</p>

<p><em>See <a href="https://youtu.be/-VCrYshE7iQ">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 12 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/11-pop-goes-the-weasel-er-count">Pop goes the...population count?</a> | <a href="/202512/13-licking-licm">Loop-Invariant Code Motion</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:armagain">
<p>We're using ARMv7 again here for simplicity and to yet again avoid vectorisation that we'll get to later in this series. <a class="footnote-backref" href="#fnref:armagain" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:bpu">
<p>In this case the compiler determined that just doing the multiply every time would be cheaper than a branch on every iteration to avoid it. Branch predictors do help this in general, but it's still better to do one branch at the beginning and specialise each loop individually. <a class="footnote-backref" href="#fnref:bpu" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/11-pop-goes-the-weasel-er-count</id>
    <title>Pop goes the...population count?</title>

    <summary>Compilers can take advantage of some very specific instructions</summary>

    <link href="http://xania.org/202512/11-pop-goes-the-weasel-er-count?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-11T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Who among us hasn't looked at a number and wondered, "How many one bits are in there?" No? Just me then?</p>

<p>Actually, this "population count" operation can be pretty useful in some cases like data compression algorithms, <a href="https://vaibhavsagar.com/blog/2019/09/08/popcount/">cryptography, chess, error correction</a>, and <a href="https://faculty.cc.gatech.edu/~echow/pubs/ics26-liuPS.pdf">sparse matrix representations</a>. How might one write some simple C to return the number of one bits in an unsigned 64 bit value?</p>

<p>One way might be to loop 64 times, checking each bit and adding one if set. Or, equivalently, shifting that bit down and adding it to a running count: sometimes the population count operation is referred to as a "horizontal add" as you're adding all the 64 bits of the value together, horizontally. There are "divide and conquer" approaches too, see the amazing <a href="https://graphics.stanford.edu/~seander/bithacks.html#CountBitsSetNaive">Stanford Bit Twiddling Hacks page</a> for a big list.</p>

<p>My favourite way is to loop while the value is non-zero, and use a cute trick to "clear the bottom set bit". The loop count is then the number of set bits. How do you clear the bottom set bit? You <code>and</code> a value with itself decremented!</p>

<div class="codehilite"><pre><span /><code>value       : 11010100
subtract 1  : 11010011
&amp; value     : 11010000
</code></pre></div>


<p>If you try some examples on paper, you'll see that subtracting one always moves the bottom set bit down by one place, setting all the bits from there down. Everything else is left the same. Then when you <code>and</code>, the bottom set bit is guaranteed to be <code>and</code>-ed with zero, but everything else remains. Great stuff!</p>

<p>All right, let's see what the compiler makes of this:</p>

<p>The core loop is pretty much what we'd expect, using the <a href="/202512/02-adding-integers">lea trick</a> to get <code>value - 1</code>, <code>and</code>ing and counting:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L3:</span>
<span class="w">  </span><span class="nf">lea</span><span class="w"> </span><span class="no">rax</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">rdi-1</span><span class="p">]</span><span class="w">          </span><span class="c1">; rax = value - 1</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">edx</span><span class="p">,</span><span class="w"> </span><span class="mi">1</span><span class="w">                </span><span class="c1">; ++result</span>
<span class="w">  </span><span class="nf">and</span><span class="w"> </span><span class="no">rdi</span><span class="p">,</span><span class="w"> </span><span class="no">rax</span><span class="w">              </span><span class="c1">; value &amp;= value - 1</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L3</span><span class="w">                   </span><span class="c1">; ...while (value)</span>
</code></pre></div>


<p>Great stuff, but we can do better. By default gcc and clang both target some kind of "generic" processor which influences which instructions they can use. We're compiling for Intel here, and gcc's default is somewhere around Intel's "nocona" architecture, from 2004. Unless you are running vintage hardware you can probably change it to something better. Let's pick the super up-to-date "westmere" (from 2010...) using <code>-march=westmere</code> and see what happens<sup id="fnref:sandy"><a class="footnote-ref" href="#fn:sandy">1</a></sup>:</p>

<p>Wow! The entire routine has been replaced with a <strong>single instruction</strong> - <code>popcnt rax, rdi</code><sup id="fnref:arm"><a class="footnote-ref" href="#fn:arm">3</a></sup>. When I first saw this optimisation I was blown away: the compiler recognises a relatively complex loop as being functionally equivalent to a single instruction. Both gcc and clang can do this, and within Compiler Explorer you can use the optimisation pipeline viewer in clang to see that clang's "loop deletion pass" is responsible for this trick:</p>

<p class="picture">
<a href="11-popcount-clang-opt.png">
<img src="11-popcount-clang-opt.png" alt="Screenshot of CE showing the opt pipeline viewer with the loop being replaced with a call to @llvm.ctpop.i64" width="500" height="276" />
</a>
<br />Compiler Explorer's <a href="https://aoco.compiler-explorer.com/z/Wra11PPP9">Opt Pipeline View</a>
</p>


<p>Compilers canonicalise code too, so some similar population count code will also be turned into a single instruction, though sadly not all. In this case, it's probably better to actually use a standard C++ routine to guarantee the right instruction as well as reveal your intention: <a href="https://en.cppreference.com/w/cpp/numeric/popcount.html"><code>std::popcount</code></a>. But even if you don't, the compiler might just blow your mind with a single instruction anyway.</p>

<p><em>See <a href="https://youtu.be/Hu0vu1tpZnc">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 11 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/10-loop-unrolling">Unrolling loops</a> | <a href="/202512/12-loop-unswitching">Unswitching loops for fun and profit</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:sandy">
<p>Interestingly, if you select other architectures like <code>-march=sandybridge</code> you'll see the compiler emits a seemingly redundant <code>xor eax, eax</code> <em>before</em> the <code>popcnt eax, edi</code>. This is to work around a performance bug in the CPU<sup id="fnref:omg"><a class="footnote-ref" href="#fn:omg">2</a></sup>! <a class="footnote-backref" href="#fnref:sandy" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:omg">
<p>The register renamer / dependency tracking in the out of order engine incorrectly believes that <code>popcnt</code> has a dependency on the previous value of the destination register. The <code>xor eax, eax</code> breaks this dependency. <a class="footnote-backref" href="#fnref:omg" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:arm">
<p>While this post is using Intel, ARM has <code>popcount</code>, and some RISC-V variants have <code>cpop</code>, which do the same thing. <a class="footnote-backref" href="#fnref:arm" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/10-loop-unrolling</id>
    <title>Unrolling loops</title>

    <summary>Learning when the compiler decides to unroll loops for performance</summary>

    <link href="http://xania.org/202512/10-loop-unrolling?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-10T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>A common theme for helping the compiler optimise is to give it as much information as possible. Using the <a href="/202512/06-dividing-to-conquer">right signedness of types</a>, targeting the right CPU model, keeping <a href="/202512/09-induction-variables">loop iterations independent</a>, and for today's topic: telling it how many loop iterations there are going to be ahead of time.</p>

<p>Taking the range-based sum example <a href="/202512/08-going-loopy">from our earlier post on loops</a>, but using a <code>std::span</code><sup id="fnref:span"><a class="footnote-ref" href="#fn:span">1</a></sup>, we can explore this ability. Let's take a look at what happens if we use a dynamically-sized span - we'd expect it to look very similar to the <code>std::vector</code> version:</p>

<p>The compiler doesn't know how many ints there will be ahead of time, and it generates pretty straightforward code<sup id="fnref:odd"><a class="footnote-ref" href="#fn:odd">3</a></sup>:</p>

<div class="codehilite"><pre><span /><code><span class="na">.LBB0_2</span>
<span class="w">  </span><span class="nf">ldr</span><span class="w"> </span><span class="no">r3</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="no">r2</span><span class="p">],</span><span class="w"> </span><span class="mi">#4</span><span class="w">      </span><span class="c1">; value = *ints++</span>
<span class="w">  </span><span class="nf">subs</span><span class="w"> </span><span class="no">r1</span><span class="p">,</span><span class="w"> </span><span class="no">r1</span><span class="p">,</span><span class="w"> </span><span class="mi">#4</span><span class="w">       </span><span class="c1">; count down remaining bytes</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">r0</span><span class="p">,</span><span class="w"> </span><span class="no">r3</span><span class="p">,</span><span class="w"> </span><span class="no">r0</span><span class="w">        </span><span class="c1">; sum = value + sum</span>
<span class="w">  </span><span class="nf">bne</span><span class="w"> </span><span class="no">.LBB0_2</span><span class="w">           </span><span class="c1">; loop if no more bytes</span>
</code></pre></div>


<p>A simple modification to the code to pass a <code>std::span&lt;int, 8&gt;</code>, so the compiler now knows it will always loop eight times:</p>

<p>The compiler takes advantage of this knowledge by unrolling the loop - it compiles the code as if all eight iterations of the loop had been written out one after another, avoiding the loop counter, and the conditional branch. That saves two instructions per loop iteration (the <code>subs</code> and the <code>bne</code>), and also allows the compiler to spot more patterns: We see that it loads the first two values at once using the "load multiple" <code>ldmib</code> instruction<sup id="fnref:pres"><a class="footnote-ref" href="#fn:pres">4</a></sup>.</p>

<p>Try changing the <code>8</code> in the example above to other values and you'll see the variety of different ways the compiler chooses to implement the loop. At 32 iterations it gets quite register-happy, even spilling onto the stack briefly to get more registers to load. At 50 iterations the optimiser gives up and falls back to regular looping<sup id="fnref:tiling"><a class="footnote-ref" href="#fn:tiling">5</a></sup>.</p>

<p>Compilers will sometimes partially unroll loops (chunking up unrolled sections in a larger loop), or even speculatively unroll when the count isn't known ahead of time. There's a ton of heuristics at play, and honestly, the compiler's guess is usually pretty good. But it's worth checking your hot loops<sup id="fnref:pgo"><a class="footnote-ref" href="#fn:pgo">6</a></sup> to see what it's doing - and if you can give it the loop count at compile time, you're setting it up for success.</p>

<p><em>See <a href="https://youtu.be/HvF3tF2efEA">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 10 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/09-induction-variables">Induction variables and loops</a> | <a href="/202512/11-pop-goes-the-weasel-er-count">Pop goes the...population count?</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:span">
<p>A span is a "pointer and a length", representing a contiguous array of values. The length can optionally be compile-time known, so <code>std::span&lt;int&gt;</code> is dynamically sized, but <code>std::span&lt;int, 8&gt;</code> is a span of 8 integers. <a class="footnote-backref" href="#fnref:span" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:arm">
<p>I'm using this older ARM on purpose: it doesn't have fancy-pants vector instructions, which I'll cover later. We can see the loop optimisation on this simple example without introducing lots of complexity. We'll get there, I promise! <a class="footnote-backref" href="#fnref:arm" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:odd">
<p>Here the compiler has done something unusual - it multiplies up the size by 4 (<code>lsl r1, r1, #2</code> in the preamble), and then counts down in fours. I don't know why it doesn't realise it can avoid the shift, and then just count down in ones. <a class="footnote-backref" href="#fnref:odd" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:pres">
<p>While it could load even more registers in one go, the compiler has made the tradeoff of getting two reads at once before starting a pattern of loading and adding, to take advantage of the instruction-level parallelism this unlocks. <a class="footnote-backref" href="#fnref:pres" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:tiling">
<p>I'm a little surprised/disappointed that it doesn't instead "chunk up" the loop into fixed-size blocks of say 16, and then loop over those 4 times to get 48, then add the last two. We'll see ways the compiler might choose to "chunk" later in the series with auto-vectorisation. <a class="footnote-backref" href="#fnref:tiling" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:pgo">
<p>Things like profile-guided optimisation (PGO) can help the compiler check its guesses are on-track: You build your program with instrumentation, run it with representative data, then feed the instrumentation output back to the compiler. Some of that data will include loop counts, which can help the optimiser. I won't be covering PGO in this series, but it's worth a look. <a class="footnote-backref" href="#fnref:pgo" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>


  <entry>
    <id>tag:xania.org,2007:202512/09-induction-variables</id>
    <title>Induction variables and loops</title>

    <summary>Compilers can rewrite loops to avoid expensive calculations</summary>

    <link href="http://xania.org/202512/09-induction-variables?utm_source=feed&amp;utm_medium=rss" />

    <updated>2025-12-09T12:00:00Z</updated>

    <content type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
<p class="ai-disclaimer">Written by me, proof-read by an LLM.
<br />Details at end.</p>

<p>Loop optimisations often surprise us. What looks expensive might be fast, and what looks clever might be slow.</p>

<p><a href="/202512/08-going-loopy">Yesterday</a> we saw how the compiler canonicalises loops so it (usually) doesn't matter how you write them, they'll come out the same. What happens if we do something a little more expensive inside the loop?</p>

<p>Let's take a look at something that looks like it would need an "expensive" multiply each loop iteration<sup id="fnref:dont"><a class="footnote-ref" href="#fn:dont">1</a></sup>:</p>

<p>Despite our "times table" seemingly requiring a multiply for each step, the compiler realises it can rephrase the loop to keep an accumulator, and add <code>table</code> each iteration instead:</p>

<div class="codehilite"><pre><span /><code><span class="nl">.L3:</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">rdx</span><span class="p">,</span><span class="w"> </span><span class="mi">4</span><span class="w">                </span><span class="c1">; advance the output pointer</span>
<span class="w">  </span><span class="nf">mov</span><span class="w"> </span><span class="no">DWORD</span><span class="w"> </span><span class="no">PTR</span><span class="w"> </span><span class="p">[</span><span class="no">rdx-4</span><span class="p">],</span><span class="w"> </span><span class="no">eax</span><span class="c1">; write "accumulator"</span>
<span class="w">  </span><span class="nf">add</span><span class="w"> </span><span class="no">eax</span><span class="p">,</span><span class="w"> </span><span class="no">edi</span><span class="w">              </span><span class="c1">; add "table" to the accumulator</span>
<span class="w">  </span><span class="nf">cmp</span><span class="w"> </span><span class="no">rdx</span><span class="p">,</span><span class="w"> </span><span class="no">rcx</span><span class="w">              </span><span class="c1">; have we reached the end?</span>
<span class="w">  </span><span class="nf">jne</span><span class="w"> </span><span class="no">.L3</span><span class="w">                   </span><span class="c1">; loop if not</span>
</code></pre></div>


<p>That "accumulator" is called an induction variable, and if you know a thing about maths<sup id="fnref:math"><a class="footnote-ref" href="#fn:math">2</a></sup>, you know we can rewrite lots of things as a series of sums. For example, if we wanted to output squares (x²) instead, we might expect the compiler to smartly turn this into a series of additions<sup id="fnref:expl"><a class="footnote-ref" href="#fn:expl">3</a></sup>, something like<sup id="fnref:haswell"><a class="footnote-ref" href="#fn:haswell">4</a></sup>:</p>

<p>But in fact, if we try it, the compiler goes back to squaring with <code>imul</code>:</p>

<p>What's going on here?! Yet again, the compiler is probably smarter than us: multiplies are pretty quick (around 3 cycles), and by keeping each loop iteration independent of the previous iteration, the processor can actually run many multiplications simultaneously.</p>

<p>In our manual induction variable code, each loop iteration requires the results of the previous loop iteration - a <em>loop carried dependency</em> - which could prevent this overlapping of calculations.</p>

<p>To test this theory, we can use <code>llvm-mca</code> - the <a href="https://llvm.org/docs/CommandGuide/llvm-mca.html">LLVM Machine Code Analyser</a>. This tool runs a CPU simulation and can show this overlapping<sup id="fnref:assume"><a class="footnote-ref" href="#fn:assume">5</a></sup> of computation. I've used it to generate a timeline of what happens on the CPU, cycle by cycle. In this view, instructions run from top to bottom, and time left to right. Each instruction's journey through the pipeline is shown: <code>D</code> is decode, <code>e</code> is execution, <code>E</code> is execution complete, <code>R</code> is retired (fully complete). <code>=</code> shows a stall. Each row shows one instruction - for example, <code>[0,2]</code> means iteration 0, instruction 2.</p>

<p>Watch how the instructions from different iterations overlap - that's the key to performance here. This trace is for the induction variable version:</p>

<div class="codehilite"><pre><span /><code>[0,0]     DeER .    .    .  .   add     rsi, 4
[0,1]     D=eER.    .    .  .   mov     dword ptr [rsi - 4], eax
[0,2]     DeE-R.    .    .  .   add     eax, edx
[0,3]     DeE-R.    .    .  .   add     edx, 2
[0,4]     .DeER.    .    .  .   cmp     rcx, rsi
[0,5]     .D=eER    .    .  .   jne     .L3
[1,0]     .DeE-R    .    .  .   add     rsi, 4
[1,1]     .D=eER    .    .  .   mov     dword ptr [rsi - 4], eax
[1,2]     . DeER    .    .  .   add     eax, edx
[1,3]     . DeER    .    .  .   add     edx, 2
[1,4]     . DeER    .    .  .   cmp     rcx, rsi
[1,5]     . D=eER   .    .  .   jne     .L3
</code></pre></div>


<p>Nearly the whole first loop iteration is decoded each cycle, and by the third cycle all of the first loop iteration is executing. The first iteration is complete on the 6th cycle, and the second by the seventh cycle, which means once things settle down, an iteration completes every 1½ cycles<sup id="fnref:ac"><a class="footnote-ref" href="#fn:ac">6</a></sup>. You can see the full trace <a href="https://aoco.compiler-explorer.com/z/ccE3Y5cGd">at this Compiler Explorer link</a>.</p>

<p>If we simulate the square-with-multiply version:</p>

<div class="codehilite"><pre><span /><code>[0,0]     DeER .    .    .    .   mov   edx, eax
[0,1]     D=eeeER   .    .    .   imul  edx, eax
[0,2]     D====eER  .    .    .   mov   dword ptr [rsi + 4*rax], edx
[0,3]     DeE----R  .    .    .   inc   rax
[0,4]     .DeE---R  .    .    .   cmp   rax, rdi
[0,5]     .D=eE--R  .    .    .   jne   .L3
[1,0]     .DeE---R  .    .    .   mov   edx, eax
[1,1]     .D=eeeER  .    .    .   imul  edx, eax
[1,2]     . D===eER .    .    .   mov   dword ptr [rsi + 4*rax], edx
[1,3]     . DeE---R .    .    .   inc   rax
[1,4]     . D=eE--R .    .    .   cmp   rax, rdi
[1,5]     . D==eE-R .    .    .   jne   .L3
</code></pre></div>


<p>On this simulated Haswell processor, the average loop still takes 1.5 cycles! Check the <a href="https://aoco.compiler-explorer.com/z/croW7esoj">full link here</a>. Despite using a more expensive multiply, and despite a loop iteration now taking 8 cycles, because each loop iteration is independent of the last, we still get the throughput of one iteration every 1½ cycles.</p>

<p>Compilers <em>are</em> smart - trust them. But know how to use tools like <a href="https://godbolt.org">Compiler Explorer</a> and <code>llvm-mca</code> to verify, and always benchmark.</p>

<p><em>See <a href="https://youtu.be/vZk7Br6Vh1U">the video</a> that accompanies this post.</em></p>

<hr />

<p><em>This post is day 9 of <a href="/AoCO2025-archive">Advent of Compiler Optimisations 2025</a>,
a 25-day series exploring how compilers transform our code.</em></p>

<p><em>← <a href="/202512/08-going-loopy">Going loopy</a> | <a href="/202512/10-loop-unrolling">Unrolling loops</a> →</em></p>

<p><em>This post was written by a human (<a href="/MattGodbolt">Matt Godbolt</a>) and reviewed and proof-read by LLMs and humans.</em></p>

<p><em>Support Compiler Explorer on <a href="https://patreon.com/c/mattgodbolt">Patreon</a>
or <a href="https://github.com/sponsors/compiler-explorer">GitHub</a>,
or by buying CE products in the <a href="https://shop.compiler-explorer.com">Compiler Explorer Shop</a></em>.</p>

<div class="footnote">
<hr />
<ol>
<li id="fn:dont">
<p>Please don't write code like this: don't write unchecked to an output array, use <code>std::span</code> or similar, or return a <code>std::vector&lt;&gt;</code>, or any number of better ways. I'm trying to balance small code output for teaching purposes against "real world" code. <a class="footnote-backref" href="#fnref:dont" title="Jump back to footnote 1 in the text">↩</a></p>
</li>
<li id="fn:math">
<p>Translation for Americans: "math". <a class="footnote-backref" href="#fnref:math" title="Jump back to footnote 2 in the text">↩</a></p>
</li>
<li id="fn:expl">
<p>The difference between adjacent squares goes up by 1, 3, 5, 7... <a class="footnote-backref" href="#fnref:expl" title="Jump back to footnote 3 in the text">↩</a></p>
</li>
<li id="fn:haswell">
<p>You might spot I'm using <code>haswell</code> tuning here: this is to keep it in line with later analysis I'm going to do. In this particular case it doesn't make a code generation difference, but it keeps the analysis in sync with the code here, and is close to the default <code>-mtune=generic</code> the compiler uses. The analysis tool we'll use assumes a much newer CPU, and that erodes the point I'm making in this particular post. <a class="footnote-backref" href="#fnref:haswell" title="Jump back to footnote 4 in the text">↩</a></p>
</li>
<li id="fn:assume">
<p>It assumes perfect branch prediction and no cache misses, which is a little hopeful but is a decent starting point. <a class="footnote-backref" href="#fnref:assume" title="Jump back to footnote 5 in the text">↩</a></p>
</li>
<li id="fn:ac">
<p>It works out as one and a half due to other constraints you can see if you look at the full trace. Computers are bonkers fast these days. <a class="footnote-backref" href="#fnref:ac" title="Jump back to footnote 6 in the text">↩</a></p>
</li>
</ol>
</div>

    </div></content>
  </entry>

</feed>
